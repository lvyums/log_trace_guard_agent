# 日志溯源卫士智能体 — 详细设计 v3.2.0

> 文档版本：v3.2.0（2026-08-05）
> 与 CHANGELOG.md、各 README、源码级完整项目说明保持同步；若有不一致，以代码实测为准。

---

# 一、全局公共底座设计（所有模块通用）

## 1.1 整体架构设计

```
┌──────────────────────────────────────────────────────────────┐
│  表现层                                                       │
│  ├─ Web 前端 (Vue3 SPA, 7 模块 20 子页面, 运维/实训双模式)      │
│  └─ CLI 终端 (菜单 + AI 对话 + 命令行模式)                     │
├──────────────────────────────────────────────────────────────┤
│  接口层 (FastAPI, 46 业务端点 + 2 管理 + 2 基础)               │
│  app/main.py → 7 个 modules/*/router.py → service.py          │
├──────────────────────────────────────────────────────────────┤
│  业务层 (7 大模块, 工厂注册 + 策略模式)                        │
│  log_parse / log_collect / script_gen / compliance /          │
│  training / log_correlate / advisory                          │
├──────────────────────────────────────────────────────────────┤
│  底座层                                                       │
│  L1 规则引擎 (core/rule_engine)  零依赖, 可穷举测试            │
│  L2 RAG (core/ai_base, 5 分片 ChromaDB, N-gram 降级)          │
│  L3 LLM (core/ai_base, DeepSeek 主力/轻量双模型)               │
│  横切: ContextManager / common 工具 / settings 配置            │
└──────────────────────────────────────────────────────────────┘
```

## 1.2 通用知识库分层设计（全局复用）

### 1.2.1 知识库技术选型（v3.2 现状）

- 存储：ChromaDB（持久化于 `data/chroma_db/`）
- 分片：5 个（KB_REGISTRY 注册表驱动）

| 分片名 | 标签 | 服务模块 |
|---|---|---|
| `log_basics` | 日志基础知识 | 日志解析 |
| `compliance` | 合规审计 | 合规审计 |
| `collection` | 日志采集 | 日志采集 / 规划咨询 |
| `scripts` | 脚本生成 | 脚本生成 |
| `cases` | 攻防实训/威胁狩猎 | 日志联合审查 / 攻防实训 |

- 导入：`kb_ingest.py` 从 `data/rule_data/*.json` 分片导入，500 字切块 + JSON 展平
- 检索 API：`RAGFactory.get_kb(name)` → `kb.retrieve(query, top_k)`
- 向量降级链：BGE(1024d) → MiniLM(384d) → N-gram(128d)；当前因 Keras3/transformers 兼容问题运行于 N-gram 降级态

## 1.3 通用 AI 调用策略（全局约束）

### 1.3.1 大模型选型（v3.2 现状）

- 服务：raytoken.com.cn OpenAI 兼容网关
- 模型：`deepseek-v4-flash`（主力 + 轻量共用）
- 配置：`LLM_BASE_URL` / `LLM_MODEL_NAME` / `LLM_TEMPERATURE=0.1` / `LLM_TIMEOUT=10` / `LLM_RETRY_COUNT=1`

### 1.3.2 统一调用接口与降级策略

```python
result = await llm.chat(messages, temperature=0.3, timeout=60)
# result = {"success": bool, "content": str, "error": str}
```

全局降级规则（所有模块一致）：
1. LLM 调用失败 → 记录 warning → 走 RAG 检索兜底
2. RAG 无结果 → 规则引擎/预置模板兜底
3. 动态场景生成失败 → `_fallback_scenario` → 传统预置场景

### 1.3.3 Prompt 管理策略

- `core/ai_base/prompt_manager.py`（Agent）+ `ai_core/prompts.py`（CLI）统一收口
- 所有 LLM 输出要求纯 JSON（无 markdown 包裹），解析失败有截断恢复（`LLMChainAnalyzer._parse_llm_json`）

## 1.4 通用数据流转规范

### 1.4.1 路径处理机制

- Agent 端：`settings._PROJECT_ROOT` 上溯 3 层定位仓库根，`rule_data_dir` / `chroma_db_path` / `upload_temp_dir` 全部基于根路径
- CLI 端：`~/.log-guard/` 用户目录（config.json / reports / es_templates.json / certs）

### 1.4.2 模块间上下文传递

- `ContextManager`（FastAPI Depends）：单请求内跨模块传递中间结果
- `ModuleContext`：记录每模块输入/输出/状态（SUCCESS/FAILED），前端跨模块联动展示
- 实训场景跨页传递：sessionStorage `current-training-scenario`

---

# 二、模块一：日志解析模块（log_parse）

## 2.1 模块核心定位

异构日志 → 设备类型识别 → 标准化字段 → 风险研判 → 字段释义，是安全分析链路入口。

## 2.2 输入源设计

- 单行日志（identify/parse/assess/explain）
- 批量日志（parse/batch）
- 日志文件上传（upload + parse/batch-file）

## 2.3 核心功能详细设计

### 2.3.1 日志类型自动识别

`service.identify()` → `ParserFactory` 多特征打分（时间戳格式/关键字/端口模式），返回 device_type：ssh / web / firewall / waf / db / traffic。

### 2.3.2 异构日志标准化字段提取

各设备解析器（`ssh_parse.py` 等 6 个）实现统一 `parse()` 接口，输出：

```
src_ip / dst_ip / user / action / method / url / protocol /
status / risk / timestamp / raw_log
```

### 2.3.3 字段释义智能问答（explain）

RAG 检索 `log_basics` 分片 + LLM 组织讲解；支持 batch。

### 2.3.4 正常/异常行为 AI 研判（assess）

规则引擎判级（risk_rules.json）+ LLM 补充解释 → P0-P3 五级风险。

## 2.4 边界与异常处理

- 空行/垃圾输入 → 友好提示（conftest 有 malformed 样例）
- 未知设备类型 → 降级为通用解析 + 提示
- 批量超限 → `max_batch_size=100` 截断提示

---

# 三、模块二：合规审计基线搭建模块（compliance）

## 3.1 模块核心定位

等保2.0 / 网安法 / 数据安全法合规问答、基线生成、自查整改。

## 3.2 输入源设计

- 合规问题（qa）
- 资产/系统清单（baseline）
- 现有配置项（check / check/batch）

## 3.3 核心功能详细设计

### 3.3.1 合规标准智能问答（qa）

RAG 检索 `compliance` 分片（compliance_standards.json）+ LLM 回答，标注来源条款。

### 3.3.2 个性化合规基线自动生成（baseline）

`baseline_gen.py` 按资产类型匹配 `compliance_baselines.json` 模板 → 生成可执行基线清单。

### 3.3.3 合规自查与缺口整改（check）

`compliance_rule.py` 逐条比对配置项 → 输出 满足/缺口 + 整改建议；支持 batch。

## 3.4 边界与异常处理

- 未知合规项 → 提示参考标准原文
- 空资产清单 → 返回通用基线

---

# 四、模块三：统一日志采集架构指导模块（log_collect + advisory）

## 4.1 模块核心定位

设备日志采集方案匹配、分层架构推荐、平台选型、故障排错。

## 4.2 输入源设计

- 设备信息（厂商/型号/协议）→ match / plan
- 企业规模/日志量 → architecture/recommend（advisory）
- 故障现象描述 → fault/diagnose

## 4.3 核心功能详细设计

### 4.3.1 设备日志方案智能匹配（match）

`device_match.py` 匹配 `device_protocol.json` + `collect_templates.json` → 输出标准化采集配置（syslog/SNMP/API）。

### 4.3.2 分层架构智能推荐（plan / architecture/recommend）

`collect_strategy.py` / `arch_strategy.py` 按规模推荐集中/分布式/混合架构（arch_templates.json）。

### 4.3.3 平台选型（advisory/platform/choose）

`platform_strategy.py` 多平台（ELK/Splunk/…）多维度打分（script_gen_platforms.json）。

### 4.3.4 采集故障智能排错（fault/diagnose）

`fault_fix.py` 关键词匹配 `fault_kb.json` → 故障原因 + 修复步骤；`fault/list` 返回常见故障列表。

## 4.4 边界与异常处理

- 未知设备 → 建议手动配置 + 相关模板推荐
- 故障库未命中 → LLM 补充分析

---

# 五、模块四：技术赋能与脚本规则生成模块（script_gen）

## 5.1 模块核心定位

正则/ES/SPL 脚本生成、优化、攻击溯源、Splunk/ES 实际执行与配置持久化。

## 5.2 输入源设计

- 日志样本 / 攻防场景描述（regex / es-query）
- 现有脚本（optimize）
- 攻击链数据（trace）
- Splunk/ES 连接信息（splunk/config、es/config）

## 5.3 核心功能详细设计

### 5.3.1 场景化正则/查询脚本生成

`regex_gen.py` + `es_sql_gen.py`：LLM 生成 + 规则库（script_gen_regex.json / script_gen_es_queries.json）兜底；支持一键对日志执行匹配测试。

### 5.3.2 脚本规则优化与讲解（optimize）

LLM 评分（script_gen_scoring.json）+ 优化建议。

### 5.3.3 Splunk/ES 实际执行（v3.2 新增）

- `splunk/search`：5 场景模板匹配（SSH爆破/SQL注入/Web攻击/异常流量/数据泄露）→ 生成 SPL → REST API 执行
- `splunk/test` / `es/test`：连接测试（Bearer Token / Basic / 无认证三方式）
- `es/search`：DSL 语法校验 + 执行
- `splunk/config` / `es/config`：配置持久化到 `.env`（企业多分析师共享、换设备不丢配置）
- 客户端：`common/splunk_client.py` / `common/es_client.py`

### 5.3.4 半自动攻击链路溯源（trace）

`trace_link.py` + `script_gen_trace_patterns.json`：按攻击链类型生成溯源检索脚本；CLI 端进一步支持报告导出 + 监控规则闭环（ES DSL + 正则 → 存模板）。

## 5.4 边界与异常处理

- LLM 生成失败 → 规则库 fallback（script_gen_fallback_rules.json）
- Splunk/ES 连接失败 → 明确错误码（CLI 退出码 0/1/2）
- 无凭据 → 提示进入连接配置

---

# 六、模块五：交互式攻防实训模块（training）

## 6.1 模块核心定位

预置 + 动态实训场景、任务下发、双维度评分、报告生成。

## 6.2 实训场景体系设计

- 预置场景：`training_scenarios.json` 12 个（basic/collection/filtering/web_attack/lateral_movement/compliance/intrusion_detection/incident_response/network_security/architecture/comprehensive 等分类）
- **动态场景**：log-correlate `/to-scenario` 注入（DYN_ 前缀），不持久化到 JSON，运行时内存管理（`TaskEngine._dynamic_scenarios` / `_dynamic_answers`）

## 6.3 核心功能详细设计

### 6.3.1 智能化任务下发引擎（task_engine.py）

```
dispatch(scenario_id?)      → 指定场景
dispatch(category?)         → 按分类
inject_scenario(scenario, standard_answers) → 动态注入 → 返回 DYN_xxx 场景 ID
get_scenario / get_tasks / get_standard_answer → 统一查询（动态优先，JSON 兜底）
```

### 6.3.2 双维度智能纠错引擎（check_strategy.py）

`CheckStrategyFactory`：关键词规则评分 + LLM 语义评分 → 综合分 + 错因分析（error_analysis.py）。

### 6.3.3 原理讲解与复盘机制

submit 后返回标准答案对照 + 知识点讲解（standard_answers 驱动）。

### 6.3.4 实训测评与报告生成（report_gen.py）

按场景/任务/学员聚合 → 实训报告（report 端点 + 前端 Report.vue）。

## 6.4 边界与异常处理

- 动态场景 LLM 失败 → `_fallback_scenario` 降级 → 传统分类下发
- 未知场景 ID → 空结果提示
- 提交未找到标准答案 → 仅 LLM 评分

---

# 七、模块六：日志联合审查模块（log_correlate）— v3.2 核心

## 7.1 模块核心定位

多源日志关联分析 → 16 种攻击链检测 → 溯源脚本 / 实训场景闭环。

## 7.2 攻击链检测引擎

- 数据：`correlation_patterns.json` 16 条（ssh_brute_to_privesc、sql_injection_chain、web_scan_to_exploit、initial_access_to_lateral、privilege_escalation、data_exfiltration、c2_beacon、ransomware_attack、internal_reconnaissance、web_attack_to_data_theft、dns_tunneling、supply_chain_attack、insider_threat、waf_alert_to_attack、auth_failure_chain、brute_force_attempt）
- 引擎：关键词匹配（multi-keyword 阶段序列）+ LLM 补充（hybrid）；关键词不足时自动降级 LLM
- 时间线：`temporal.py` 构建阶段时间线（timestamp/stage/keyword/src_ip）
- 风险等级：`AttackChainMatcher._SEVERITY_MAP` 映射 critical/major → P1/P2

## 7.3 攻击链 → 实训场景闭环（/to-scenario）

```
POST /api/v1/log-correlate/to-scenario
  log_lines + chain_name + chain_description + chain_data(可选)
        │
        ▼ LogCorrelateService.to_training_scenario (service.py L717)
  chain_data 存在?
    ├─ 是 → TemporalAnalyzer.generate_training (LLM 按攻击数据定制)
    │        ├─ 成功 → scenario + 4 tasks + standard_answers
    │        └─ 失败 → _fallback_scenario (基于 chain_data 构造 4 任务)
    └─ 否 → 按 chain_name 关键词映射分类 → TrainingService.dispatch_tasks
        │
        ▼ TaskEngine.inject_scenario → DYN_ 场景注入
        ▼ 前端 Analyze.vue「下发实训场景」→「立即进入实训」
          → sessionStorage current-training-scenario
          → #/training/submit 答题 → submit 双维度评分
```

动态任务模板（4 任务）：T01 攻击类型识别 / T02 攻击阶段分析 / T03 关键证据提取 / T04 处置建议。

## 7.4 边界与异常处理

- 空日志/空 chain → 明确提示
- LLM 场景生成异常 → 降级传统模式（category_map 15 类关键词映射）
- 大文件 >100MB → FileCrunch 分片建议

---

# 八、模块七：规划咨询模块（advisory）

## 8.1 模块核心定位

采集架构推荐、平台选型、指导手册生成（轻咨询能力）。

## 8.2 核心功能

| 端点 | 能力 |
|---|---|
| architecture/recommend | 按规模推荐架构（策略模式 arch_strategy.py） |
| platform/choose | 多平台打分对比（platform_strategy.py） |
| guide/generate | 定制化指导手册（LLM + 模板） |

## 8.3 边界与异常处理

- 输入信息不足 → 默认中等规模推荐
- LLM 失败 → 模板兜底

---

# 九、模块联动整体流程设计（最终闭环）

## 9.1 完整链路流程

```
采集规划 → 日志解析 → 风险研判 → 关联分析(攻击链) → 溯源脚本/实训场景
   ↑          │            │              │                │
   └── 故障诊断 ┘            └── 合规自查 ──┘                └── 实训报告
```

## 9.2 任意入口接入支持

- 运维模式：轻量指引，任意模块直接使用
- 实训模式：强制分步引导 + 评分
- 全局引导（GlobalTour）5 步标准工作流

## 9.3 模块间数据传递规范

| 传递物 | 通道 | 示例 |
|---|---|---|
| 解析中间结果 | ContextManager | 设备类型 → 采集方案 |
| 攻击链数据 | 请求体 | correlate → to-scenario |
| 实训场景 | sessionStorage | Analyze.vue → Submit.vue |
| 连接配置 | .env / config.json | splunk/config → splunk/search |

---

# 十、专业工程化代码目录架构

## 10.1 整体架构设计思想

解耦 + 设计模式 + 复用：Core 底座层（全局能力收口）→ Common 工具层（100% 复用）→ Modules 业务层（高内聚低耦合）。

## 10.2 Agent 项目目录结构（v3.2）

```
log_trace_guard_agent/
├── app/                    # 应用层
│   ├── main.py             # FastAPI 入口 (179 行)
│   ├── settings.py         # 全局配置 (189 行)
│   ├── admin.py            # 管理端点 (170 行)
│   ├── exceptions.py       # 统一异常 (59 行)
│   ├── dependencies.py     # 依赖注入 (59 行)
│   └── schemas/            # Pydantic 模型 (6 文件)
├── core/                   # 核心底座
│   ├── abstract/           # 抽象接口
│   ├── ai_base/            # LLM / RAG / 向量 / Prompt / 知识导入
│   ├── rule_engine/        # 规则引擎 (regex/filter/risk)
│   ├── context_manager.py  # 跨模块上下文
│   └── data_preprocess.py  # 数据预处理
├── common/                 # 公共工具 (12 文件, 含 splunk/es 客户端)
├── modules/                # 7 大业务模块
│   ├── log_parse/          # 日志解析 (9 端点)
│   ├── log_collect/        # 日志采集 (5 端点)
│   ├── script_gen/         # 脚本生成 (13 端点)
│   ├── compliance/         # 合规审计 (4 端点)
│   ├── training/           # 攻防实训 (4 端点)
│   ├── log_correlate/      # 日志联合审查 (8 端点)
│   └── advisory/           # 规划咨询 (3 端点)
├── data/
│   ├── rule_data/          # 20 个 JSON 规则文件
│   └── chroma_db/          # 向量库持久化
├── frontend/               # Vue3 SPA (7 模块 20 子页面)
├── scripts/dev/            # mock 联调环境 + 回归脚本
├── tests/                  # 161 个测试
└── docs/                   # 部署/开发规范/配置说明
```

## 10.3 CLI 项目目录结构（v3.2）

```
log_trace_guard_cli/
├── log_guard/
│   ├── cli.py              # 双模式交互 (2038 行)
│   ├── ai_core/            # LLM/意图/编排/RAG/Prompt (8 文件)
│   ├── common/             # 工具 (utils.py 311 行)
│   ├── core/               # log_reader.py (486 行)
│   ├── modules/            # 5 业务模块 (log_parse/log_collect/script_gen/compliance/log_correlate)
│   └── data/rule_data/     # 16 个 JSON
└── tests/                  # 243 passed + 1 skipped
```

## 10.4 关键设计模式落地

| 模式 | 落地 |
|---|---|
| 工厂注册 | ParserFactory / LLMFactory / RAGFactory / CheckStrategyFactory |
| 策略模式 | collect_strategy / arch_strategy / platform_strategy / script_strategy |
| 单一职责 | 每模块 strategy 文件独立职责（如 regex_gen 只管正则生成） |
| 依赖倒置 | 业务依赖 core 抽象接口，不依赖具体 LLM/向量实现 |
| 降级容错 | LLM→RAG→规则三级降级，动态→fallback→传统两级降级 |

---

# 十一、CLI 智能体设计（v3.2）

## 11.1 双模式

- 菜单模式：主菜单 7 项；脚本生成子菜单 6 项（正则/ES/SPL/溯源/优化/连接配置）
- AI 对话：意图分类 6 类（log_parse/collection/compliance/script_gen/correlation/general），置信度阈值过滤 → orchestrator 编排 → polisher 润色

## 11.2 命令行模式（脚本集成）

`--splunk-test / --splunk-search / --es-test / --es-search`，退出码 0=成功 / 1=连接失败 / 2=DSL 非法，支持 `--json`。

## 11.3 script-gen 四大增强

1. 溯源报告导出（Markdown/JSON → `~/.log-guard/reports/`）
2. ES 查询模板管理（`~/.log-guard/es_templates.json`，内置 SSH 爆破/SQL 注入模板）
3. 溯源 → 监控规则闭环（自动生成 ES DSL + 正则规则）
4. Splunk SPL 生成 + REST API 一键执行（5 场景中文名匹配）

---

# 十二、测试策略（v3.2 现状）

## 12.1 分层次测试策略

| 层 | 策略 | 现状 |
|---|---|---|
| 规则引擎层 | 可穷举测试（纯函数断言） | test_regex_rule 等 150 个模块测试 |
| RAG 召回层 | 质量评估（检索命中率） | 手动评估 + N-gram 降级说明 |
| 大模型输出层 | 结构化断言（mock LLM） | test_to_scenario mock LLMFactory |
| API 集成层 | AsyncClient 全链路 | test_api 11 个 + test_to_scenario API 2 个 |

## 12.2 测试结果（v3.2）

- Agent：**161 passed**（145 + 16 新增 to-scenario 链路）
- CLI：**243 passed + 1 skipped**（skip = Splunk 连通性）
- 联调回归：test_api_connect 18 项 / test_cli_connect 16 项（mock Splunk:18089 + ES:19200）

## 12.3 已知限制（v3.2）

1. BGE 嵌入不可用（Keras3/transformers 兼容），N-gram 降级
2. DeepSeek API 偶发 402 → LLM 自动降级
3. Splunk 连通性测试需外部服务（mock 替代）
4. log_correlate 单文件 >100MB 建议分片
5. 「保存到 .env」依赖写权限

---

*本文档为 v3.2.0 全量重写版，替代 v2.0 历史版本；配套文档：源码级完整项目说明 / CHANGELOG / 测试报告 / dev_standard。*
