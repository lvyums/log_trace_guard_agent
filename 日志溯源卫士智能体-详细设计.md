# 日志溯源卫士智能体\-详细设计

# 一、全局公共底座设计（所有模块通用）

## 1\.1 整体架构设计

采用「**规则引擎兜底 \+ RAG知识库精准召回 \+ 大模型智能推理**」三层通用架构，所有业务模块统一复用该底座，保证功能一致性、低幻觉、高落地性。

- 第一层：结构化规则层（固定正则、固定流程、固定基线，保证基础功能100%准确）
  - 规则匹配优先级：精准匹配 > 模糊匹配 > 默认兜底规则，按优先级逐级尝试
  - 规则冲突解决：相同匹配域命中多条规则时，按**"高危害优先 \+ 具体优先"**原则裁决——危害等级高的规则优先，危害等级相同时匹配条件更具体的规则优先
  - 规则热加载：支持 `/core/rule_engine/rules/` 目录下规则文件的动态热加载，监听文件修改事件自动重载规则缓存，无需重启服务

- 第二层：RAG召回层（检索安全垂直知识库，杜绝大模型幻觉，输出行业标准方案）
  - 检索失败降级：当RAG检索相关性得分低于阈值(0.6)或检索结果为空时，跳过RAG层，直接返回规则层结果，并标注"未检索到知识库匹配内容"
  - 检索重试：RAG服务异常（网络超时/连接失败）时最多重试2次，间隔1秒递增；均失败则跳过RAG层，仅使用规则层+LLM层
  - 知识库无匹配约束：无知识库匹配内容时不调用大模型编造答案，直接输出规则层结果并标注"未命中RAG知识库，结果仅供参考"

- 第三层：大模型推理层（场景化润色、个性化适配、逻辑纠错、内容结构化输出）
  - 调用超时：默认LLM调用超时10秒，超时后直接返回规则层+RAG层结果，标注"大模型推理超时"
  - 调用异常降级：LLM返回异常（空输出/非结构化内容/HTTP错误）时，自动重试1次；仍失败则回退至规则层+RAG层拼接结果
  - 输出校验：对LLM输出结果进行结构化校验（JSON Schema验证/必填字段检查），不符合时走降级路径

## 1\.2 通用知识库分层设计（全局复用）

将知识库分为5大独立分片，模块按需精准召回，避免跨领域干扰：

- 日志基础库：全类型日志字段释义、正常/异常行为特征、设备日志样例
- 合规审计库：等保2\.0、网安法、数据安全法、审计基线、留存规范
- 采集架构库：多设备采集协议、配置规范、架构方案、故障排错手册
- 技术脚本库：攻击正则、检索语法、清洗规则、平台选型对比
- 实训案例库：标准化攻防场景、任务流程、标准答案、常见错误库

### 1\.2\.1 知识库技术选型规范

所有知识库分片统一遵循以下技术规范：

**向量数据库**：ChromaDB（本地轻量部署，无需额外基础设施）
**Embedding模型**：BAAI/bge-large-zh-v1\.5（中文语义向量，768维）
  - 备选：moka-ai/m3e-large（国产替代，中文场景表现稳定）
  - Embedding缓存：对高频查询的embedding结果做LRU缓存（最大1000条），减少重复计算

**文本分块策略**：
  - 分块大小：512 tokens（兼顾细粒度精度与上下文完整性）
  - 重叠窗口：128 tokens（避免关键信息被切分边界截断）
  - 分块粒度：按Markdown章节标题作为自然分界点，跨章节不合并

**检索策略**：
  - Top-K取值：默认返回Top-5，可根据模块类型调整（基础问答类Top-3，技术脚本类Top-5，案例类Top-5）
  - 重排序：使用Cross-encoder（BAAI/bge-reranker-large）对召回结果重排序，提升准确率
  - MMR多样性：当同一模块多个检索请求时，启用MMR(λ=0\.7)保证检索结果多样性
  - 混合检索：关键词BM25 \+ 向量语义检索双通道，权重比3:7

## 1\.3 通用AI调用策略（全局约束）

### 1\.3\.1 大模型选型规范

- **主力模型**：DeepSeek-V3 / 通义千问2\.5-72B（中文日志场景理解能力强，成本可控）
- **轻量备选**：DeepSeek-R1-Distill-Qwen-7B / Qwen2\.5-7B-Instruct（高频低复杂度场景降本）
- **切模型标准**：复杂推理（溯源链路分析/合规评估）使用主力模型；简单分类/字段提取使用轻量模型
- **模型切换入口**：统一在 `core/ai_base/llm_factory.py` 中通过配置切换，业务模块不感知

### 1\.3\.2 Prompt管理策略

- **全局模板层**：`core/ai_base/prompt_manager.py` 统一管理所有Prompt模板
- **分层组合**：
  - 全局System Prompt：定义智能体角色定位、输出规范、知识库引用约束
  - 模块级Prompt：各业务模块自行维护场景Prompt，全局注入模块标识和上下文信息
  - 冲突解决：模块Prompt覆盖全局Prompt中同名字段时，以模块Prompt为准，模块未定义时回退全局配置
- **版本管理**：Prompt模板与代码一同版本控制，每个模块Prompt标注版本号，变更时输出diff日志

### 1\.3\.3 全局约束

- 固定低温度值（默认T=0\.1），保证输出严谨、标准化，杜绝随机创作
- 强制引用检索知识库内容，无知识库匹配内容不随意生成
- 所有输出结果结构化：分点、分层、可直接落地、可直接实训校验

## 1\.4 通用数据流转规范

用户输入/文件上传 → 数据预处理（清洗、去空、格式统一）→ 规则引擎初筛 → RAG精准召回 → 大模型整合推理 → 结构化结果输出 → 日志留存/实训打分

### 1\.4\.1 模块间上下文传递机制

为支撑模块间联动，在 `core/` 层增设**统一上下文管理器**：

- **Context对象**：`core/context_manager.py` 维护全局请求上下文，包含请求ID、用户标识、输入源类型、中间处理结果、异常标注链
- **数据传递格式**：模块间传递标准化JSON Schema，包含 `module_id`、`input`、`output`、`status`、`error_info` 五个固定字段
- **状态追溯**：每个模块在处理后向Context写入处理状态（success/partial/warning/error），下游模块通过Context感知上游结果
- **任意入口支持**：用户可从任一模块开始工作（如直接提问"帮我审计合规"），上下文管理器自动初始化缺失的依赖模块数据为"未处理"状态

# 二、模块一：日志基础知识解析模块

全站基础支撑模块，负责**异构日志标准化解析、字段释义、行为研判、风险标注**，解决“看不懂日志、分不清正常/异常行为”核心问题，同时支撑企业运维研判、学员基础实训。

## 2\.2 输入源设计

- 文本输入：单条日志粘贴、日志问题咨询

- 文件输入：批量日志TXT、结构化日志文件

- 场景输入：指定设备类型（防火墙/WAF/SSH/数据库）

## 2\.3 核心功能详细设计

### 2\.3\.1 日志类型自动识别逻辑

通过特征规则库匹配识别日志归属设备：基于关键字特征、字段结构、协议格式，自动区分SSH登录日志、Web访问日志、WAF攻击日志、防火墙流量日志、数据库审计日志、系统操作日志，无人工选择也可自动分类。

### 2\.3\.2 异构日志标准化字段提取

统一解析所有杂乱日志，标准化输出固定核心字段：时间戳、源IP、目的IP、源端口、目的端口、访问账号、访问URL、请求载荷、操作命令、返回状态、设备类型。对缺失字段自动标注“字段缺失”，对乱码、特殊字符自动清洗转义。

### 2\.3\.3 字段释义智能问答逻辑

用户咨询任意日志字段、日志类型、参数含义时，优先召回「日志基础库」对应标准释义，大模型做通俗化翻译，输出“专业定义\+通俗解释\+业务场景\+风险说明”四层内容，适配零基础学员和运维新手。

### 2\.3\.4 正常/异常行为AI研判逻辑

构建双基线判定体系：固定规则匹配已知攻击，AI行为基线识别隐性异常。对比日志行为与标准正常基线、攻击特征库，自动判定行为类型，标注风险点位、攻击名称、危害程度、可疑依据。

**基线更新机制**（解决"新攻击出现后如何更新基线"问题）：
- 版本化管理：每条基线带有版本号和生效时间戳，支持历史版本回溯
- 被动更新：运营人员通过管理后台新增/修改/下线规则，修改后触发基线热加载
- 主动更新（规划）：系统周期性（默认每7天）扫描历史告警中"已确认攻击但未命中基线"的样本，自动生成新规则建议，经人工确认后纳入基线库
- 更新影响范围：基线更新时，Context记录当前会话使用的基线版本，保证同一请求全链路版本一致

## 2\.4 边界与异常处理

- 日志格式错乱、无特征：提示无法识别，给出标准日志上传模板

- 字段大量缺失：标注缺失字段，不盲目研判风险

- 疑似误报：标注“疑似风险”，不直接判定高危，给出复核建议

## 2\.5 双场景落地形态

企业运维：快速解析告警日志、定位异常行为、辅助初步研判；教学实训：学员上传日志，系统自动批改行为判定结果，讲解错误原因。

# 三、模块二：合规审计基线搭建模块

## 3\.1 模块核心定位

解决企业日志管理**不合规、无审计基线、无自查标准、整改无方向**问题，同时实现网安合规审计岗位全流程实训。

## 3\.2 输入源设计

- 企业资产信息：外网业务、服务器数量、数据库、运维设备

- 现有日志管理现状：存储方式、留存时长、审计机制

- 合规问题咨询：留存周期、防篡改、审计范围等

## 3\.3 核心功能详细设计

### 3\.3\.1 合规标准智能问答

基于合规知识库，精准回答等保、网安法、数安法关于日志的硬性要求：留存时长、必须留存日志类型、防篡改要求、审计频次、责任人制度，杜绝网络错误合规知识。

### 3\.3\.2 个性化合规基线自动生成

根据用户资产规模、业务类型，自动匹配专属监控基线，包含六大核心监控场景：异常登录监控、异地IP登录监控、端口扫描监控、数据库批量操作监控、高危命令执行监控、异常外联监控。同时输出基线阈值、检查频率、告警标准。

### 3\.3\.3 合规自查与缺口整改

对比用户现有运维现状与合规标准，自动筛查合规缺口，输出问题清单、风险等级、具体整改步骤、制度模板、配置规范，形成可直接交付审计的合规材料。

## 3\.4 边界与异常处理

- 用户资产信息不全：生成通用基线\+个性化补充建议

- 特殊行业（教育/园区/政企）：自动适配对应行业合规细则

- 无日志设备场景：提示设备补全、日志采集整改建议

# 四、模块三：统一日志采集架构指导模块

## 4\.1 模块核心定位

解决企业日志**分散孤岛、格式杂乱、不会部署集中采集、排查采集故障**问题，输出可直接落地的采集方案与架构设计。

## 4\.2 输入源设计

- 设备类型：防火墙、WAF、IDS/IPS、堡垒机、服务器、数据库

- 企业规模：小微园区、中小企业、大型政企

- 故障场景：日志丢失、格式错乱、无法入库、推送失败

## 4\.3 核心功能详细设计

### 4\.3\.1 设备日志方案智能匹配

根据设备型号、日志样例、设备类型，自动匹配最优采集协议：Syslog推送、文件采集、数据库日志同步、流量探针采集，输出适配该设备的专属采集方案。

### 4\.3\.2 分层架构智能推荐

根据企业日志量级、设备数量、预算、运维能力，自动推荐架构：小微场景推荐轻量化单机日志汇聚架构，中大型场景推荐ELK分布式集群架构，同时讲解组网逻辑、数据流向、存储备份策略。

### 4\.3\.3 标准化采集配置指导

输出完整落地配置规范：设备端推送配置、服务端接收配置、日志格式解析规则、字段映射标准、存储目录规划，全程手把手流程化指导，无需用户自主摸索。

### 4\.3\.4 采集故障智能排错

基于故障知识库，针对日志丢失、格式乱、时间错位、传输中断、无法入库等问题，自动定位故障原因、输出排查步骤、修复方案、预防策略。

## 4\.4 边界与异常处理

- 小众设备无模板：输出通用Syslog采集适配方案\+自定义格式适配方法

- 网络环境受限：提供离线采集\+定时同步替代方案

# 五、模块四：技术赋能与脚本规则生成模块

## 5\.1 模块核心定位

项目核心生产力模块，替代人工编写正则、检索语句、清洗规则、溯源脚本，同时提供平台技术选型赋能，大幅降低日志溯源技术门槛。

## 5\.2 输入源设计

- 攻击场景：SSH爆破、SQL注入、内网扫描、异常外联、数据导出

- 工具需求：ELK检索、SIEM规则、日志清洗、数据统计

- 架构需求：日志分析平台选型、新旧架构对比

## 5\.3 核心功能详细设计

### 5\.3\.1 场景化规则/脚本生成

根据用户指定攻防场景，自动生成行业标准正则匹配规则、ES检索语句、日志清洗逻辑、统计分析规则。所有生成内容基于沉淀的安全脚本知识库，保证可用性、准确性，无无效冗余内容。

### 5\.3\.2 脚本规则优化与讲解

支持用户自主提交编写的正则、检索语句、脚本，系统自动纠错、优化、精简，同时逐行讲解逻辑、适用场景、优缺点，适配学员技能提升需求。

### 5\.3\.3 日志平台智能选型赋能

对比轻量日志系统、ELK、商用SIEM的适配场景、成本、运维难度、性能上限，结合企业实际情况智能推荐最优技术方案，规避选型过度或性能不足问题。

### 5\.3\.4 半自动攻击链路溯源

基于多源日志关键字段关联逻辑，自动梳理攻击时序、攻击入口、跳板资产、受害资产、攻击阶段，输出结构化攻击链路，辅助人工快速完成溯源报告。

## 5\.4 边界与异常处理

- 小众攻击场景：生成通用规则\+自定义修改指引

- 用户脚本错误：精准定位错误位置、错误原因、修复逻辑

# 六、模块五：交互式攻防实训模块

## 6\.1 模块核心定位

项目差异化核心模块，实现**无人值守、全自动、可纠错、可测评**的日志溯源实训，解决传统网安实训无反馈、不落地、无实战的痛点。

## 6\.2 实训场景体系设计

搭建阶梯式实训场景，从基础到实战逐层递进：日志基础认知场景、日志采集配置场景、日志清洗筛查场景、Web攻击溯源场景、内网渗透溯源场景、合规审计整改场景。

## 6\.3 核心功能详细设计

### 6\.3\.1 智能化任务下发引擎

系统自动加载场景资源，为学员下发标准化实操任务，明确每一步操作目标、操作内容、交付结果，复刻企业真实运维工作流，杜绝无效实训。

### 6\.3\.2 双维度智能纠错引擎

采用「规则精准匹配\+语义相似度比对」双校验机制，对学员提交的规则、脚本、溯源结论、整改方案进行校验：精准识别完全错误、部分遗漏、逻辑偏差、规则不严谨四类问题，定位具体错误点位。

**评分阈值体系**：
  - 语义相似度阈值：`≥0.85` 判定为正确，`0.70-0.84` 判定为部分正确（输出优化建议），`<0.70` 判定为错误（输出正确标准+原理解释）
  - 规则精准匹配：完全匹配（100%）→ 完全正确；部分匹配字段（≥60%字段命中）→ 部分遗漏；核心字段缺失 → 逻辑偏差
  - 综合评级：A（≥90分）直接通过；B（70-89分）提示优化；C（<70分）判错并强制复盘

**学员提交格式规范**：
  - 提交内容统一封装为JSON对象，包含 `submit_type`（rule/script/conclusion/plan）、`content`（实际提交内容）、`scenario_id`（场景ID）
  - 系统自动补全session上下文，无需学员手动提供背景信息

**跨模块接口契约**：
  - 实训模块调用其它模块数据时，通过Context请求上游模块的缓存结果；如上游模块结果不存在，返回"请先完成前置模块任务"提示
  - 前置模块结果以标准化JSON Schema传递：`{ "module_id": "log_parse", "output": {...}, "status": "success" }`

### 6\.3\.3 原理讲解与复盘机制

纠错后自动输出：错误原因、正确标准、底层攻防原理、岗位实操要点、优化建议，让学员知其然更知其所以然，实现技能沉淀。

### 6\.3\.4 实训测评与报告生成

全程记录学员操作轨迹、正确率、错误类型，自动分析学员薄弱项（正则能力弱、溯源链路不全、合规认知不足等），自动生成标准化实训报告与能力提升方案。

## 6\.4 边界与异常处理

- 学员操作偏差：容错处理，优先提示优化而非直接判错

- 答案不唯一：判定逻辑合理性，不刻板对照标准答案

# 七、模块联动整体流程设计（最终闭环）

## 7\.1 完整链路流程（采集→解析→赋能→合规→实训）

1\. 模块三（采集架构）：实现日志标准化汇聚，产出规范数据源

2\. 模块一（日志解析）：完成日志识别、字段提取、风险初判

3\. 模块四（技术赋能）：生成筛查规则、溯源脚本，梳理攻击链路

4\. 模块二（合规基线）：完成风险合规判定、输出整改方案

5\. 模块五（实训交互）：将全流程封装为实训任务，实现人才培养闭环

## 7\.2 任意入口接入支持

用户无需从模块一开始，可从任意模块独立接入：

- 用户直接提问"帮我写一条SQL注入的ES检索语句" → 路由到模块四，上下文管理器自动将模块一/三状态标记为"本次未使用"
- 用户直接上传日志文件问"这些日志合规吗" → 路由到模块二，上下文管理器自动调用模块一完成日志解析后再进入模块二
- 核心原则：用户看到的是统一智能体，不感知模块边界；模块内部通过Context自动补全依赖

## 7\.3 模块间数据传递规范

所有跨模块数据传递统一使用标准化JSON Schema：

```json
{
  "module_id": "log_parse",
  "input": { "raw_log": "...", "source": "upload" },
  "output": {
    "parsed_fields": { "timestamp": "...", "src_ip": "...", "dst_ip": "..." },
    "risk_assessment": { "level": "high", "attack_type": "sql_injection" },
    "field_completeness": 0.85
  },
  "status": "success",
  "error_info": null,
  "meta": { "request_id": "req_xxx", "module_version": "1.0.0", "timestamp": "2026-07-13T10:00:00Z" }
}
```

## 7\.4 异常传递处理

- 上游模块输出为空或状态为error时，下游模块根据依赖类型处理：
  - **强依赖**（如模块二依赖模块一的解析结果）：阻断链路，返回上游错误信息，引导用户修复
  - **弱依赖**（如模块五依赖模块一/二的分析结果但可独立运行）：继续执行并标注"部分数据不可用"
- 错误信息链：Context中error_info字段以链式结构记录所有上游错误，终端用户可见第一条可读错误

# 八、整体开发落地优先级

**Phase 1a（基础底座先行，2-3周）**：公共底座（Core层LLM工厂/RAG工厂/Prompt管理）\+ Common工具层 \+ 项目目录结构搭建 \+ 模块一基础版（支持SSH登录日志+Web访问日志2种类型）

**Phase 1b（底座扩展，2-3周）**：模块一完整版（全部6种日志类型）\+ 模块三（采集架构指导，含故障排错知识库）

**二期（核心亮点，3-4周）**：模块四（脚本生成\+溯源赋能）

**三期（增值能力，2-3周）**：模块二（合规审计基线）

**四期（差异化能力，3-4周）**：模块五（交互式实训纠错）

# 九、专业工程化代码目录架构（解耦\+设计模式\+复用）

## 9\.1 整体架构设计思想

本项目采用**分层架构 \+ 单一职责 \+ 依赖倒置 \+ 工厂模式 \+ 策略模式**组合设计，彻底实现：业务模块解耦、AI能力解耦、工具类全局复用、知识库能力可插拔、便于迭代扩展、前后端接口统一标准化。

核心设计原则：

- 分层隔离：数据层、服务层、业务层、AI层、工具层完全隔离，互不侵入

- 统一复用：所有通用能力（日志解析、RAG调用、正则、文件处理）抽离公共工具包

- 策略模式：不同日志类型、不同攻击场景、不同合规规则采用独立策略实现，可自由扩展

- 工厂模式：统一生成RAG实例、解析器实例、实训校验器实例，统一管理

- 高内聚低耦合：五大业务模块独立存在，可单独启停、单独迭代、单独替换

## 9\.2 完整项目目录结构（企业级标准）

```Plain Text
log_trace_guard_agent/
├── app/                        # 项目主程序入口
│   ├── main.py                 # FastAPI主入口、路由总挂载
│   ├── settings.py             # 全局配置（大模型Key、向量库路径、阈值配置）
│   ├── exceptions.py           # 全局统一异常捕获、异常封装
│   └── dependencies.py         # 全局依赖注入
├── core/                       # 核心底座层（全局固定底座）
│   ├── ai_base/                # AI大模型&RAG核心底座
│   │   ├── llm_factory.py      # 大模型工厂模式（统一创建LLM实例）
│   │   ├── rag_factory.py      # RAG知识库工厂（分库精准召回）
│   │   ├── prompt_manager.py   # 全局Prompt模板管理、策略
│   │   ├── vector_store.py     # 向量库初始化、持久化管理
│   │   └── embedding_cache.py  # Embedding结果LRU缓存
│   ├── rule_engine/            # 规则引擎底座
│   │   ├── regex_rule.py       # 正则规则统一管理
│   │   ├── risk_baseline.py    # 风险基线、合规基线池
│   │   ├── filter_rule.py      # 日志过滤、降噪规则
│   │   └── rules/              # 规则文件目录（支持热加载）
│   ├── context_manager.py      # 模块间上下文传递管理
│   └── data_preprocess.py      # 全局数据预处理底座
├── common/                     # 全局工具复用层（所有模块共用）
│   ├── logger.py               # 统一日志输出
│   ├── file_util.py            # 文件解析、上传、读写工具
│   ├── ip_util.py              # IP解析、归属地工具
│   ├── time_util.py            # 时间格式化工具
│   ├── str_util.py             # 字符串清洗、转义、匹配
│   └── result_util.py          # 统一接口返回封装
├── modules/                    # 五大业务模块（完全解耦、独立开发）
│   ├── __init__.py
│   ├── log_parse/              # 模块一：日志基础解析模块
│   │   ├── parser_factory.py   # 解析器工厂（策略模式：多日志类型解析分发）
│   │   ├── ssh_parse.py
│   │   ├── web_parse.py
│   │   ├── waf_parse.py
│   │   ├── firewall_parse.py
│   │   ├── db_parse.py
│   │   ├── service.py          # 模块业务逻辑
│   │   └── router.py           # 模块接口路由
│   ├── compliance/             # 模块二：合规审计基线模块
│   │   ├── compliance_rule.py
│   │   ├── baseline_gen.py
│   │   ├── service.py
│   │   └── router.py
│   ├── log_collect/            # 模块三：日志采集架构模块
│   │   ├── collect_strategy.py # 采集策略模式（不同设备不同方案）
│   │   ├── device_match.py
│   │   ├── fault_fix.py
│   │   ├── service.py
│   │   └── router.py
│   ├── script_gen/             # 模块四：技术赋能脚本生成模块
│   │   ├── script_strategy.py  # 场景脚本生成策略
│   │   ├── regex_gen.py
│   │   ├── es_sql_gen.py
│   │   ├── platform_choose.py
│   │   ├── trace_link.py
│   │   ├── service.py
│   │   └── router.py
│   └── training/               # 模块五：交互式实训模块
│       ├── task_engine.py
│       ├── check_strategy.py   # 双维度校验策略
│       ├── error_analysis.py
│       ├── report_gen.py
│       ├── service.py
│       └── router.py
├── data/                       # 项目数据目录
│   ├── chroma_db/              # 向量库存放路径
│   ├── case_data/              # 实训案例库
│   ├── rule_data/              # 规则基线库
│   └── upload_temp/            # 临时上传文件
├── static/                     # 静态资源、前端页面资源
├── tests/                      # 单元测试、模块测试
│   ├── conftest.py             # 测试夹具（Mock LLM/Mock ChromaDB）
│   ├── test_rules/             # 规则引擎单元测试
│   └── test_data/              # 测试样本数据
│       └── rules/              # 标准化日志测试样本（按设备类型分类）
├── docs/                       # 项目设计文档、接口文档
├── .env                        # 环境变量配置
├── requirements.txt            # 依赖版本锁定
└── README.md

```

## 9\.3 各分层核心职责与解耦设计说明

### 9\.3\.1 Core 核心底座层（全局能力统一收口）

该层为整个智能体的**最底层能力底座**，所有业务模块禁止自行初始化LLM、RAG、规则，全部统一调用Core层，保证全局AI参数统一、规则统一、数据预处理统一。

核心设计亮点：

- 工厂模式：LLM工厂、RAG工厂统一生产实例，后续切换模型、新增知识库无需改业务代码

- 策略模式：不同风险规则、过滤规则独立配置，可动态开启/关闭

- 彻底解耦：业务模块只调用能力，不关心底层实现
- 上下文管理器：`context_manager.py` 统一维护模块间数据传递和状态追溯，业务模块不直接依赖其它模块

### 9\.3\.2 Common 公共工具层（全局100%复用）

将所有高频通用能力抽离工具类，避免代码冗余、重复造轮子，所有模块统一调用，保证输出格式、清洗规则、返回格式全局统一。

复用范围：日志解析、合规检测、脚本生成、实训校验全场景通用。

### 9\.3\.3 Modules 业务模块层（高内聚低耦合）

五大核心业务模块完全独立：

- 每个模块拥有独立路由、独立服务逻辑、独立策略、独立数据

- 模块之间禁止直接互相导入依赖

- 跨模块联动通过**统一调度层**未来扩展，不破坏解耦

每个模块内部统一三层结构：**策略/规则层 → service业务层 → router接口层**，标准化程度极高，便于团队分工开发。

## 9\.4 关键设计模式落地说明（对应业务场景）

### 9\.4\.1 工厂模式落地

应用场景：日志解析器工厂、RAG知识库工厂、大模型实例工厂。

价值：新增设备日志类型、新增知识库分片、切换大模型，无需改动业务代码，直接扩展工厂即可。

### 9\.4\.2 策略模式落地

应用场景：

- 多类型日志解析策略（SSH/Web/WAF/防火墙各自独立解析策略）

- 多设备采集方案策略（不同设备匹配不同采集方案）

- 多攻击场景脚本生成策略（爆破/注入/横向移动各自生成规则）

- 实训答案校验策略（文本校验/规则校验/语义校验）

价值：场景无限扩展、互不干扰、便于维护迭代。

### 9\.4\.3 单一职责原则落地

工具只做工具、AI底座只做AI、业务只做业务、路由只做接口分发，无臃肿大类、无臃肿函数。

### 9\.4\.4 依赖倒置原则落地

业务模块依赖抽象底座、抽象工具，不依赖具体实现，底层更新完全不影响上层业务。

## 9\.5 目录架构适配项目优势

- **适配五大模块解耦开发**

- **适配迭代节奏**：一期、二期、三期、四期功能可分批接入，不影响整体架构

- **适配AI能力迭代**：模型升级、知识库扩容、Prompt优化全部在core层完成

- **适配实训\+运维双场景**：业务层完全隔离双场景逻辑，互不干扰

- **企业级可上线标准**：目录规范、解耦规范、设计模式规范可直接用于商用项目交付

# 十、测试策略

## 10\.1 分层次测试策略

### 10\.1\.1 规则引擎层（可穷举测试）

- **单元测试覆盖**：每条规则编写独立TestCase，覆盖正常匹配、边界值（空输入/超长输入/特殊字符）、不匹配三种场景
- **回归测试**：规则变更时自动运行全部规则测试套件，保证修改不引入副作用
- **测试数据**：在 `tests/test_data/rules/` 目录维护标准化测试日志样本，按设备类型分类

### 10\.1\.2 RAG召回层（质量评估）

- **自动化指标**：构建标注测试集（至少200条Query-Answer对），每次知识库更新后自动化评估Recall@N（N=5时≥85%）、MRR（≥0\.75）
- **人工抽检**：每周抽检20条实际用户Query的召回结果，记录"Top-5含正确答案"比例，跟踪低于80%的模块进行知识库优化
- **回归基线**：维护召回质量基线版本，知识库变更前后对比指标，防止退化

### 10\.1\.3 大模型输出层（结构化断言测试）

- **Schema验证**：每个模块大模型输出定义JSON Schema，自动化测试验证输出格式合法性
- **内容回归**：维护50+条标准输入-输出对，每次Prompt变更后运行回归测试，人工对比输出质量变化
- **幻觉检测**：随机抽样10%的线上请求日志，人工标注LLM输出是否包含无知识库依据的编造内容

## 10\.2 CI集成建议

- 每次提交自动运行规则层单元测试和RAG召回指标测试
- 每次发版前完成大模型输出回归测试和人工抽检
- 使用 `tests/` 目录下 `conftest.py` 统一管理测试夹具（Mock LLM/Mock ChromaDB），加速测试执行
