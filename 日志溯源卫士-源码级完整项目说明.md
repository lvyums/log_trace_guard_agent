# 日志溯源卫士 (Log Trace Guard) v3.2.0 — 基于源代码的完整项目说明

> 文档版本：v3.2.0（2026-08-05）
> 适用范围：`log_trace_guard_agent/`（Web 智能体）+ `log_trace_guard_cli/`（CLI 精简版）
> 本说明以代码实测为准，与 CHANGELOG.md / 各 README 保持同步。

---

## 一、项目全景

日志溯源卫士是面向安全运营/教学的模块化智能体，分两个子项目：

| 子项目 | 定位 | 技术栈 | 模块数 |
|---|---|---|---|
| `log_trace_guard_agent/` | Web 智能体（FastAPI + Vue3 SPA） | FastAPI / Pydantic / ChromaDB / Vue3 / Element Plus | 7 大业务模块、46 个 API 端点 |
| `log_trace_guard_cli/` | 终端版智能体（菜单 + AI 对话） | Python / prompt_toolkit / requests | 5 大业务模块 + AI 对话 |

两者共享同一套业务设计理念：**规则引擎 → RAG 知识库 → LLM 推理** 三层通用架构，工厂注册模式驱动模块扩展。

```
┌─────────────────────────────────────────────────────────┐
│                    前端 (Vue3 SPA)                        │
│  7 导航模块 / 20 子页面 / 运维+实训双模式 / 风险等级可视化  │
└──────────────────────────┬──────────────────────────────┘
                           │ HTTP
┌──────────────────────────▼──────────────────────────────┐
│              Web 智能体 (FastAPI, :8000)                  │
│  7 模块 × (strategy/engine → service → router) 工厂注册    │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│  三层通用架构                                            │
│  L1 规则引擎: regex_rule / filter_rule / risk_baseline    │
│  L2 RAG: 5 个知识库分片 (ChromaDB, N-gram 降级)           │
│  L3 LLM: DeepSeek (raytoken.com.cn) 轻量/主力双模型       │
└─────────────────────────────────────────────────────────┘
```

---

## 二、三层通用架构的代码实现

三层架构贯穿所有业务模块，代码位于 `core/` 与 `core/ai_base/`。

### 2.1 第一层：规则引擎

代码位置：`core/rule_engine/`（3 个文件，纯 Python 零依赖）

| 文件 | 职责 |
|---|---|
| `regex_rule.py` | 正则规则匹配 — 每个业务规则 = pattern + action + risk 映射 |
| `filter_rule.py` | 过滤规则 — 批量日志行按条件筛选（时间/级别/来源） |
| `risk_baseline.py` | 风险基线 — 将规则命中映射为 P0-P3 风险等级 |

典型用法（日志解析模块）：

```python
# modules/log_parse/parser_factory.py 内部
parser = ParserFactory.get_parser(device_type)   # 工厂注册模式
fields = parser.parse(log_line)                  # 正则提取字段
risk = RiskBaseline.evaluate(fields)             # 风险等级判定
```

### 2.2 第二层：RAG 知识库

代码位置：`core/ai_base/`（vector_store.py / rag_factory.py / kb_ingest.py）

**知识库分片（5 个，KB_REGISTRY 注册）：**

| 分片名 | 标签 | 用途 |
|---|---|---|
| `log_basics` | 日志基础知识 | 日志解析模块问答 |
| `compliance` | 合规审计 | 等保2.0 / 网安法问答 |
| `collection` | 日志采集 | 采集方案/架构指导 |
| `scripts` | 脚本生成 | 正则/ES 查询知识 |
| `cases` | 攻防实训/威胁狩猎 | 攻击链溯源、实训场景 |

**检索 API（v3.2 实测）：**

```python
from core.ai_base.rag_factory import RAGFactory

kb = RAGFactory.get_kb("log_basics")          # 获取知识库实例
result = kb.retrieve(query="syslog 格式", top_k=3)   # 检索
```

**向量存储降级链（vector_store.py）：**

```
Level 1: BGE 中文语义嵌入 (BAAI/bge-large-zh-v1.5, 1024 维)   ← 首选
Level 2: MiniLM 轻量英文 (384 维)                             ← 不可用时
Level 3: N-gram 纯 Python (128 维)                            ← 当前降级态
```

> ⚠️ 已知限制：Keras 3 与 transformers 不兼容（缺 `tf-keras`），BGE 嵌入暂不可用，当前以 N-gram 降级运行。见测试报告「已知限制」。

**数据导入（kb_ingest.py）：** `ingest_all(rule_data_dir, force)` 从 `data/rule_data/*.json` 分片导入；`_chunk_text` 按 500 字切块、`_flatten_json` 展平嵌套 JSON。

### 2.3 第三层：LLM 推理

代码位置：`core/ai_base/llm_factory.py`（208 行）

**工厂注册模式：**

```python
LLMFactory.register("main", DeepSeekClient)     # 主力模型
LLMFactory.register("light", LightweightClient) # 轻量模型
```

**配置（app/settings.py）：**

| 配置项 | 值 |
|---|---|
| `llm_base_url` | `https://raytoken.com.cn/v1` |
| `llm_model_name` / `llm_light_model_name` | `deepseek-v4-flash` |
| `llm_temperature` | 0.1 |
| `llm_timeout` | 10s |
| `llm_retry_count` | 1 |

**统一接口 `BaseLLMClient.chat()`：** 返回 `{"success": bool, "content": str, "error": str}`，所有模块一律经此接口调用，LLM 失败自动降级（规则/RAG 兜底）。

### 2.4 跨模块抽象接口

代码位置：`core/abstract/`（log_parse.py）— 定义解析器抽象基类，各设备解析器实现统一 `parse()` 接口，供工厂注册。

### 2.5 数据预处理

代码位置：`core/data_preprocess.py` — 日志行清洗（去 ANSI、去首尾空白、syslog 头剥离），统一在解析入口调用。

### 2.6 跨模块上下文

代码位置：`core/context_manager.py`（121 行）— `ContextManager` 在单次请求内跨模块传递中间结果（如解析出的设备类型 → 采集方案模块），`ModuleContext` 记录每个模块的输入/输出/状态，支撑前端「跨模块联动」展示。

---

## 三、Agent 模块代码详解（7 大业务模块）

每个模块遵循统一结构：`strategy/engine 文件 → service.py → router.py → __init__.py`，端点经 `app/main.py` 统一挂载。

### 3.1 模块一：日志解析 (`modules/log_parse/`)

**文件：** `base_parser.py`（抽象基类）、`parser_factory.py`（工厂）、`ssh_parse.py` / `web_parse.py` / `firewall_parse.py` / `waf_parse.py` / `db_parse.py` / `traffic_parse.py`（6 类设备解析器）、`service.py`、`router.py`

**核心能力：**
- 日志类型自动识别（identify）— 多特征打分，返回 device_type
- 异构日志标准化字段提取（parse）— 6 类设备统一输出 src_ip / dst_ip / user / action / risk 等字段
- 字段释义智能问答（explain / explain/batch）— RAG 检索 + LLM 讲解
- 正常/异常行为 AI 研判（assess）— 规则引擎判级 + LLM 补充解释
- 批量解析（parse/batch / parse/batch-file / upload）

**端点 9 个：** `POST /identify`、`POST /parse`、`POST /assess`、`POST /explain`、`POST /explain/batch`、`POST /parse/batch`、`POST /upload`、`POST /parse/batch-file`、`POST /cleanup`

### 3.2 模块二：日志采集 (`modules/log_collect/`)

**文件：** `device_match.py`（设备匹配）、`collect_strategy.py`（采集策略）、`fault_fix.py`（故障修复）、`service.py`、`router.py`

**核心能力：**
- 设备日志方案智能匹配（match）— 按厂商/型号/协议匹配采集模板
- 分层架构智能推荐（plan / plan/batch）— 按企业规模推荐集中/分布式架构
- 采集配置生成 — 输出标准化采集配置（syslog / SNMP / API 等）
- 采集故障智能排错（fault/diagnose + fault/list）— 关键词匹配故障库

**端点 5 个：** `POST /match`、`POST /plan`、`POST /plan/batch`、`POST /fault/diagnose`、`GET /fault/list`

### 3.3 模块三：脚本生成 (`modules/script_gen/`)

**文件：** `regex_gen.py`、`es_sql_gen.py`、`trace_link.py`、`platform_choose.py`、`script_strategy.py`（策略抽象）、`service.py`、`router.py`

**核心能力（v3.2 已扩展至 13 端点）：**
- 场景化正则规则生成（regex / regex/batch）— LLM 生成 + 一键测试
- ES 查询语句生成（es-query / es-query/batch）
- 攻击链溯源脚本生成（trace）— 生成溯源检索脚本
- 脚本优化（optimize）— LLM 评分 + 优化建议
- **Splunk SPL 生成 + 实际执行**（splunk/search、splunk/open-url）— 5 场景模板匹配，REST API 一键执行
- **Splunk/ES 连接测试与配置持久化**（splunk/test、splunk/config、es/test、es/config）
- **ES 查询实际执行**（es/search）— DSL 语法校验 + 执行

**端点 13 个：** `POST /regex`、`POST /regex/batch`、`POST /es-query`、`POST /es-query/batch`、`POST /trace`、`POST /optimize`、`POST /splunk/search`、`POST /splunk/open-url`、`POST /splunk/test`、`POST /splunk/config`、`POST /es/search`、`POST /es/test`、`POST /es/config`

### 3.4 模块四：合规审计 (`modules/compliance/`)

**文件：** `compliance_rule.py`（合规规则）、`baseline_gen.py`（基线生成）、`service.py`、`router.py`

**核心能力：**
- 合规标准智能问答（qa）— RAG 检索等保2.0/网安法/数据安全法
- 个性化合规基线自动生成（baseline）— 按资产信息生成基线报告
- 合规自查与缺口整改（check / check/batch）— 配置项逐条比对，输出整改建议

**端点 4 个：** `POST /qa`、`POST /baseline`、`POST /check`、`POST /check/batch`

### 3.5 模块五：攻防实训 (`modules/training/`)

**文件：** `task_engine.py`（任务引擎）、`check_strategy.py`（评分策略）、`error_analysis.py`（错题分析）、`report_gen.py`（报告生成）、`service.py`、`router.py`

**核心能力：**
- 任务下发（dispatch）— 预置 12 个场景 + 动态场景（DYN_ 前缀）
- **动态场景注入（inject_scenario）** — 由 log-correlate 攻击链生成专属实战场景，注入后可被 dispatch / get_scenario / get_standard_answer 访问
- 答案提交与双维度校验（submit）— 关键词规则评分 + LLM 语义评分
- 流式分析（analyze-stream）— SSE 流式返回分析过程
- 实训报告生成（report）

**端点 4 个：** `POST /dispatch`、`POST /submit`、`POST /analyze-stream`、`POST /report`

### 3.6 模块六：日志联合审查 (`modules/log_correlate/`)

**文件：** `service.py`（842 行，核心）、`temporal.py`（时间线分析 + 实训场景生成）、`router.py`

**核心能力：**
- 多源日志关联分析（correlate）— 关键词 + LLM 双引擎检测攻击链
- **16 种攻击链模式**（`correlation_patterns.json`）— 覆盖 SSH 爆破提权、SQL 注入、Web 扫描到利用、横向移动、数据窃取、C2、勒索、DNS 隧道、供应链、内部威胁、WAF 绕过、认证失败等
- 大文件分片分析（file-crunch + upload）— 超 100MB 建议分片
- **攻击链 → 溯源脚本**（to-trace）
- **攻击链 → 实训场景**（to-scenario）— 动态生成实战场景（LLM 失败降级 fallback）
- 攻击链模式列表（patterns）

**端点 8 个：** `POST /correlate`、`POST /file-crunch`、`POST /cleanup`、`POST /to-trace`、`POST /to-scenario`、`POST /upload`、`POST /file-crunch-cleanup`、`GET /patterns`

### 3.7 模块七：规划咨询 (`modules/advisory/`)

**文件：** `arch_strategy.py`（架构推荐策略）、`platform_strategy.py`（平台选型策略）、`service.py`、`router.py`

**核心能力：**
- 日志采集架构推荐（architecture/recommend）— 按企业规模/日志量推荐架构
- 日志平台选型（platform/choose）— 多平台对比打分
- 定制化指导手册生成（guide/generate）

**端点 3 个：** `POST /architecture/recommend`、`POST /platform/choose`、`POST /guide/generate`

---

## 四、CLI 工具代码详解（`log_trace_guard_cli/`）

CLI 为 Web 智能体的精简版：**保留 5 大业务模块 + AI 对话**，已删除 training 模块与 script_gen 平台选型（约 1500 行）。

### 4.1 双模式交互 (`cli.py`, 2038 行)

```
模式一：传统菜单（输入数字）
  主菜单 8 项：选择日志文件 / 日志解析 / 日志采集 / 脚本生成 / 合规审计 / 联合日志审查 / AI 对话 / 退出
  脚本生成子菜单 6 项：正则生成 / ES 查询生成 / Splunk SPL 生成 / 攻击溯源 / 脚本优化 / 连接配置(ES+Splunk 双入口)

模式二：AI 智能对话（输入自然语言）
  /menu 切回菜单模式；意图分类 6 类：log_parse / collection / compliance / script_gen / correlation / general
```

**命令行模式（脚本集成，退出码契约）：**

| 命令 | 说明 | 退出码 |
|---|---|---|
| `log-guard --splunk-test` | 测试 Splunk 连接 | 0=成功 1=失败 |
| `log-guard --splunk-search '<SPL>'` | 执行 SPL 搜索 | 0=成功 1=失败 2=DSL 非法 |
| `log-guard --es-test` | 测试 ES 连接 | 0=成功 1=失败 |
| `log-guard --es-search '<DSL>'` | 执行 ES 查询 | 0=成功 1=失败 2=DSL 非法 |

均支持 `--json` 输出。

### 4.2 AI 智能核心 (`ai_core/`)

| 文件 | 职责 |
|---|---|
| `settings.py`（176 行） | 配置管理（读取 `~/.log-guard/config.json`） |
| `llm_client.py`（197 行） | LLM 客户端（DeepSeek OpenAI 兼容） |
| `intent_classifier.py`（79 行） | 意图分类器（6 类，LLM 调用 + 置信度阈值） |
| `orchestrator.py`（220 行） | 编排器（意图 → 业务模块 → 回复润色） |
| `rag_engine.py`（272 行） | RAG 检索（规则降级） |
| `prompts.py`（172 行） | Prompt 管理 |
| `polisher.py`（71 行） | 回复润色 |
| `context.py`（120 行） | 对话上下文管理 |

### 4.3 CLI 业务模块（5 个模块，共 ~5600 行）

| 模块文件 | 行数 | 能力 |
|---|---|---|
| `modules/log_parse.py` | 1247 | 识别/解析/研判/释义（与 Web 对齐） |
| `modules/log_collect.py` | 1045 | 采集方案/架构/故障排错 |
| `modules/script_gen.py` | 1623 | 正则/ES/SPL/溯源/优化 + 模板管理 |
| `modules/compliance.py` | 691 | 合规问答/基线/自查 |
| `modules/log_correlate.py` | 971 | 攻击链检测/溯源报告导出/监控规则闭环 |

**script-gen 增强（CLI v3.0+）：**
- 溯源报告导出：Markdown/JSON → `~/.log-guard/reports/`
- ES 查询模板管理：`~/.log-guard/es_templates.json`（内置 SSH 爆破 / SQL 注入模板）
- 溯源 → 监控规则闭环：分析一次攻击 → 自动生成 ES DSL + 正则规则 → 可存为模板
- Splunk SPL 生成 + 一键执行：5 场景（SSH爆破 / SQL注入 / Web攻击 / 异常流量 / 数据泄露），中文场景名匹配

### 4.4 CLI 数据与配置

- `data/rule_data/`：16 个 JSON 规则文件（与 Agent 端 20 个相比，少了 training 场景/答案 2 个 + platform 2 个）
- 连接配置：`~/.log-guard/config.json`（ES / Splunk 双入口，None 存空串）
- 命令配置：`log-guard --splunk-test` 等 4 命令；退出码 0/1/2

---

## 五、前端代码详解（`frontend/`）

### 5.1 技术栈

Vue 3 + TypeScript + Vite + Element Plus + Vue Router（跨模块状态用 `utils/crossModuleStore.ts`）+ axios

### 5.2 导航配置（`src/config.ts`）

**7 导航模块、20 子页面**（以 `children` 加总为准）：

| 模块 | 子页面数 | 子页面 |
|---|---|---|
| 日志解析 | 4 | 日志识别 / 结构化解析 / 风险研判 / 批量解析 |
| 日志联合审查 | 2 | 关联分析 / 攻击链模式 |
| 规划咨询 | 4 | 采集方案 / 架构推荐 / 平台选型 / 指导手册 |
| 故障诊断 | 1 | 故障诊断 |
| 脚本生成 | 4 | 正则生成 / ES查询生成 / 攻击溯源 / 脚本优化 |
| 合规审计 | 3 | 合规问答 / 基线生成 / 合规自查 |
| 攻防实训 | 2 | 实训场景 / 实训报告 |

### 5.3 组件架构

- **`App.vue`** — 主布局（左侧导航 + 顶栏 + 运维/实训双模式切换 + 全局引导）
- **通用组件 `components/`** — FileUpload（批量上传）、RiskBadge/RiskCard（风险等级可视化）、CodeBlock（代码展示）、SplunkSettings（Splunk/ES 连接配置面板）、KnowledgePanel（RAG 知识面板）、GlobalTour（引导）等 17 个
- **业务页面 `modules/`** — 每模块一组 Vue 页面（共 36 个 .vue 文件）
- **API 层 `api.ts`** — axios 封装全部后端端点

### 5.4 运维/实训双模式 UI

- 顶栏一键切换：运维模式（轻量操作指引）/ 实训模式（强制分步引导 + 评分）
- `GlobalTour.vue` 提供 5 步安全日志分析标准工作流引导

### 5.5 风险等级可视化

`config.ts` 定义 P0-P3 + normal 五级配色，RiskBadge / RiskCard 组件统一渲染。

### 5.6 跨模块联动

- 日志解析结果自动传递给采集方案、攻击溯源等模块（`utils/crossModuleStore.ts`）
- 攻击链分析页（`Analyze.vue`）支持「下发实训场景」→ sessionStorage 存场景 → 跳转实训答题页（`training/Submit.vue`）

---

## 六、JSON 数据文件详情

### Agent 端（`data/rule_data/`，20 个）

| 文件 | 用途 |
|---|---|
| `correlation_patterns.json` | **16 条攻击链模式**（名称/描述/severity/patterns） |
| `training_scenarios.json` | 12 个预置实训场景 |
| `training_standard_answers.json` | 预置场景标准答案 |
| `log_features.json` | 日志特征库（识别用） |
| `risk_rules.json` | 风险规则 |
| `device_protocol.json` | 设备协议库 |
| `collect_templates.json` | 采集模板 |
| `arch_templates.json` | 架构模板 |
| `fault_kb.json` | 故障知识库 |
| `compliance_standards.json` / `compliance_baselines.json` | 合规标准/基线 |
| `script_gen_regex.json` / `script_gen_es_queries.json` / `script_gen_trace_patterns.json` / `script_gen_scene_keywords.json` / `script_gen_scoring.json` / `script_gen_time_map.json` / `script_gen_fallback_rules.json` | 脚本生成规则库 |
| `script_gen_platforms.json` / `script_gen_platform_fallback.json` | 平台选型库 |

### CLI 端（`data/rule_data/`，16 个）

去掉 training 场景/答案（2 个）+ 平台选型（2 个），其余 16 个与 Agent 端一致。

---

## 七、应用层代码详解（`app/`）

| 文件 | 行数 | 职责 |
|---|---|---|
| `main.py` | 179 | FastAPI 主入口：挂载 7 模块路由 + 根路径/健康检查 + CORS |
| `settings.py` | 189 | 全局配置（Pydantic BaseSettings，`.env` 驱动） |
| `admin.py` | 170 | 管理端点：`/api/v1/admin/reload`（重载规则）、`/api/v1/admin/metrics`（指标） |
| `exceptions.py` | 59 | 统一异常处理 |
| `dependencies.py` | 59 | 依赖注入（ContextManager） |
| `schemas/` | 6 文件 | Pydantic 数据模型（log_parse / log_collect / log_correlate / context_schema / risk_level） |

**关键配置（settings.py）：** `service_port=8000`、`chroma_db_path=data/chroma_db`、`embedding_model=BAAI/bge-large-zh-v1.5`、Splunk 6 项（base_url/username/password/auth_token/verify_ssl/search_timeout/max_results）、ES 6 项（base_url/username/password/verify_ssl/search_timeout/max_results）。

---

## 八、公共工具层（`common/`）

| 文件 | 职责 |
|---|---|
| `result_util.py` | 统一返回封装 `{code, msg, data, timestamp}` |
| `logger.py` | 日志管理（LogManager） |
| `json_util.py` | JSON 加载（含截断恢复） |
| `batch_util.py` | 批量处理 |
| `file_util.py` | 文件工具 |
| `ip_util.py` / `str_util.py` / `time_util.py` | IP / 字符串 / 时间工具 |
| `splunk_client.py` | Splunk REST API 客户端（Bearer Token / Basic 双认证） |
| `es_client.py` | Elasticsearch 客户端（DSL 校验 + 执行） |

---

## 九、测试体系

### Agent 测试（161 个，全通过）

- `tests/test_api.py` — 11 个 FastAPI 集成测试（health / identify / parse / assess）
- `tests/test_rules/` — 150 个模块测试：

| 文件 | 用例数 |
|---|---|
| `test_script_gen.py` | 52 |
| `test_log_collect.py` | 32 |
| `test_advisory.py` | 19 |
| **`test_to_scenario.py`（新增）** | **16** |
| `test_db_parse.py` | 7 |
| `test_firewall_parse.py` | 6 |
| `test_web_parse.py` | 5 |
| `test_ssh_parse.py` | 5 |
| `test_waf_parse.py` | 5 |
| `test_regex_rule.py` | 3 |

> 新增 `test_to_scenario.py` 覆盖攻击链→实训场景全链路：fallback 降级、LLM 场景生成（成功/markdown/失败）、TaskEngine 动态注入、service 双模式、API 集成。

### CLI 测试（243 passed + 1 skipped）

`tests/` 13 个文件，12 个 pytest 文件共 244 用例（`test_all_features_capture.py` 收集 0 用例）；1 个跳过 = Splunk 连通性（需外部服务，可用 `scripts/dev/mock_services.py`）。

### 回归工具

- `scripts/dev/mock_services.py` — mock Splunk(:18089) + mock ES(:19200)，全本地零依赖
- `scripts/dev/test_api_connect.py` — 18 项 Web 联调回归
- `scripts/dev/test_cli_connect.py` — 16 项 CLI 联调回归

---

## 十、设计模式汇总

| 模式 | 应用位置 |
|---|---|
| 工厂注册 | `ParserFactory`（解析器）、`LLMFactory`（模型）、`RAGFactory`（知识库）、`CheckStrategyFactory`（评分策略） |
| 策略模式 | `collect_strategy` / `arch_strategy` / `platform_strategy` / `script_strategy` |
| 三层架构 | 规则引擎 → RAG → LLM 贯穿所有业务模块 |
| 依赖注入 | FastAPI Depends + `ContextManager` 跨模块上下文 |
| 降级容错 | LLM 失败 → RAG 兜底 → 规则降级；动态场景失败 → fallback → 传统场景 |

---

## 十一、打包与分发

- Agent 端：`uvicorn app.main:app --host 0.0.0.0 --port 8000` 启动；前端 `npm run build` 产物在 `frontend/dist/`
- CLI 端：`setup.py` / `pyproject.toml`，命令 `log-guard`；数据目录随包分发
- 前端顶栏含 CLI 下载横幅（CliDownloadBanner.vue）

---

## 十二、配置管理

### Agent 端（settings.py，189 行）

`.env` 驱动（`_PROJECT_ROOT` 上溯 3 层定位仓库根），Splunk/ES 配置可通过系统设置面板保存到 `.env`（`POST /splunk/config`、`/es/config`）。生产注意：Docker 容器内写权限可能丢失，建议环境变量注入。

### CLI 端（ai_core/settings.py，176 行）

`~/.log-guard/config.json`，含 ES / Splunk 连接配置（None 存空串）。

---

## 十三、已知限制

1. BGE 嵌入不可用（Keras 3 与 transformers 不兼容，需 `tf-keras`），当前 N-gram 降级
2. DeepSeek API 偶发 402（欠费），LLM 层自动降级规则/RAG 兜底
3. N-gram 非语义向量，精度低于语义嵌入
4. Splunk 连通性测试依赖外部服务（可用 mock_services.py 替代）
5. log_correlate 单文件 >100MB，FileCrunch >30s，建议分片
6. 「保存到 .env」依赖文件写权限

---

## 十四、API 端点总览（46 个业务端点 + 2 管理 + 2 基础）

### 基础

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/` | 服务信息（version=3.2.0） |
| GET | `/health` | 健康检查 `{"code":200,"data":{"status":"healthy"},"msg":"success"}` |

### 管理（app/admin.py）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/v1/admin/reload` | 重载规则 |
| GET | `/api/v1/admin/metrics` | 运行指标 |

### 模块一：日志解析 (9)

`POST /api/v1/log-parse/identify`、`/parse`、`/assess`、`/explain`、`/explain/batch`、`/parse/batch`、`/upload`、`/parse/batch-file`、`/cleanup`

### 模块二：日志采集 (5)

`POST /api/v1/log-collect/match`、`/plan`、`/plan/batch`、`/fault/diagnose`、`GET /fault/list`

### 模块三：脚本生成 (13)

`POST /api/v1/script-gen/regex`、`/regex/batch`、`/es-query`、`/es-query/batch`、`/trace`、`/optimize`、`/splunk/search`、`/splunk/open-url`、`/splunk/test`、`/splunk/config`、`/es/search`、`/es/test`、`/es/config`

### 模块四：合规审计 (4)

`POST /api/v1/compliance/qa`、`/baseline`、`/check`、`/check/batch`

### 模块五：攻防实训 (4)

`POST /api/v1/training/dispatch`、`/submit`、`/analyze-stream`、`/report`

### 模块六：日志联合审查 (8)

`POST /api/v1/log-correlate/correlate`、`/file-crunch`、`/cleanup`、`/to-trace`、`/to-scenario`、`/upload`、`/file-crunch-cleanup`、`GET /patterns`

### 模块七：规划咨询 (3)

`POST /api/v1/advisory/architecture/recommend`、`/platform/choose`、`/guide/generate`

---

## 十五、攻击链 → 实训场景链路（v3.2 关键闭环）

```
log-correlate/correlate (关键词+LLM 双引擎检测 16 种攻击链)
        │ 检出攻击链 chain_data
        ▼
log-correlate/to-scenario (POST)
        │ LogCorrelateService.to_training_scenario()
        ├─ chain_data 存在 → TemporalAnalyzer.generate_training(LLM 定制场景)
        │     ├─ 成功 → 4 任务场景 (类型识别/阶段分析/证据提取/处置建议)
        │     └─ LLM 失败 → _fallback_scenario 降级 (基于 chain_data 构造)
        └─ chain_data 不存在 → TrainingService.dispatch_tasks 按分类下发预置场景
        ▼
TaskEngine.inject_scenario() → DYN_ 前缀场景注入 → dispatch/submit 可访问
        ▼
前端 Analyze.vue「立即进入实训」→ sessionStorage → training/Submit.vue 答题
```

关键代码位置：`service.py` L717（to_training_scenario）、`temporal.py` L487/L686（generate_training / _fallback_scenario）、`task_engine.py` L163（inject_scenario）。

---

*本文档与 CHANGELOG.md、agent/README.md、CLI/README.md、测试报告.md 保持同步；若发现不一致，以代码实测为准。*
