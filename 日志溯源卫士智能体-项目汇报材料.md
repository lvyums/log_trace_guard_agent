# 日志溯源卫士智能体 (Log Trace Guard) — 项目汇报材料

> **项目版本**: v3.0.0
> **开发周期**: 2026-07-14 ~ 2026-07-24（10 天）
> **代码规模**: 236 个文件变更，38,058 行代码新增
> **测试覆盖**: 369 个单元测试全部通过
> **提交记录**: 78 次 Git 提交

---

## 一、项目定位与价值

**日志溯源卫士**是一个 AI 驱动的安全日志分析与网络安全实训平台，核心解决三个行业痛点：


| 痛点       | 传统方式                   | 本项目方案                                      |
| ---------- | -------------------------- | ----------------------------------------------- |
| 日志看不懂 | 人工逐行分析，依赖经验     | 5 种解析器自动识别 + 结构化提取 + 风险研判      |
| 合规无方向 | 查文档、问专家，效率低     | 等保/网安法/数安法知识库 + 智能自查 + 整改建议  |
| 实训无反馈 | 传统网安实训无纠错、不落地 | 双维度校验引擎 + 自动评分 + 原理讲解 + 能力报告 |

---

## 二、项目整体架构

### 2.1 双形态产品

项目同时提供 **Web 智能体** 和 **CLI 命令行工具** 两种形态：

```
┌─────────────────────────────────────────────────────────────┐
│                    日志溯源卫士 v3.0                          │
├────────────────────────────┬────────────────────────────────┤
│   Web 智能体               │   CLI 工具                      │
│   FastAPI + Vue3 + TS      │   Python 3.8+ 命令行           │
│   端口 8000 + 前端 5173     │   log-guard 命令入口            │
│   29 个 REST API            │   16 个命令行参数               │
│   ChromaDB 向量库           │   轻量级本地 JSON 缓存          │
│   5,000+ 行 Vue/TS          │   ~20,000 行 Python             │
└────────────────────────────┴────────────────────────────────┘
```

### 2.2 五层分层架构

```
┌──────────────────────────────────────────────────────────┐
│  app/         FastAPI 路由 + 中间件 + Pydantic 校验       │
│  modules/     5 大业务模块（零耦合，模块间无直接导入）     │
│  core/        LLM 工厂 + 规则引擎 + RAG 知识库 + 上下文   │
│  common/      日志/文件/JSON/结果 等全局工具               │
│  data/        21 个 JSON 规则文件 + ChromaDB 向量库        │
└──────────────────────────────────────────────────────────┘
```

### 2.3 三层通用推理架构（核心设计）

所有模块统一复用「**规则引擎兜底 + RAG 知识库精准召回 + 大模型智能推理**」三层架构：

```
用户请求
  │
  ▼
┌──────────────┐    匹配成功     ┌──────────────┐
│  规则引擎     │ ──────────────▶ │  直接返回     │
│  (JSON/正则)  │                └──────────────┘
└──────┬───────┘
       │ 匹配不足
       ▼
┌──────────────┐    检索到知识    ┌──────────────┐
│  RAG 知识库   │ ──────────────▶ │  补充返回     │
│  (ChromaDB)  │                └──────────────┘
└──────┬───────┘
       │ 无知识 / 需深度解读
       ▼
┌──────────────┐    生成回答      ┌──────────────┐
│  LLM 推理     │ ──────────────▶ │  智能返回     │
│  (DeepSeek)  │                └──────────────┘
└──────────────┘
```

**设计原则**：

- 第一层保证 100% 基础功能准确，不依赖任何外部服务
- 第二层精准召回安全领域知识，杜绝大模型幻觉
- 第三层仅在前两层不足时触发，做场景化润色和深度解读
- LLM 调用失败时自动降级回规则 + RAG 兜底，系统始终可用

---

## 三、五大核心功能模块

### 3.1 模块一：日志解析（6 个 API 端点）

**解决的问题**：异构日志格式杂乱，人工分析效率低、易遗漏


| 功能             | 说明                                                      | 技术实现                                                          |
| ---------------- | --------------------------------------------------------- | ----------------------------------------------------------------- |
| 日志类型自动识别 | 自动区分 SSH/Web/WAF/防火墙/数据库日志                    | 规则引擎 + 多特征加权评分 + RAG 补充 + LLM 兜底                   |
| 结构化字段提取   | 从原始日志提取 18+ 个标准字段（IP、端口、用户、操作等）   | 5 种专用解析器（SSH/Web/WAF/Firewall/DB），继承 BaseParser 抽象类 |
| 风险等级研判     | 自动判定 P0-P3 风险等级 + 攻击类型 + 修复建议             | 7 条风险规则 + 4 种匹配算子 + 置信度评分                          |
| 批量解析         | 一次提交多条日志，批量识别+解析+风险研判一体化            | 逐条解析 + 统计摘要（high/medium/low/noise）                      |
| 字段释义         | 对任意日志字段提供「专业定义+通俗解释+业务场景+风险说明」 | RAG 从日志基础库检索字段释义                                      |

**5 种日志解析器**：


| 解析器         | 识别关键词                    | 核心能力                                                   |
| -------------- | ----------------------------- | ---------------------------------------------------------- |
| SSHParser      | sshd, failed password, sudo   | 6 个正则 pattern，检测 rm/chmod/passwd/wget/curl 高危命令  |
| WebParser      | HTTP/1., GET /, Mozilla       | 3 种日志格式（Combined/Common/Simple ISO）                 |
| WAFParser      | waf, blocked, attack detected | 11 种攻击类型中英映射（sqli→SQL注入、xss→XSS跨站脚本等） |
| FirewallParser | drop, in=, src=, block        | 4 种格式（iptables/pf/商业防火墙/syslog）                  |
| DBParser       | mysql, postgres, oracle       | 15 种危险 SQL 关键词检测（drop/truncate/grant/revoke 等）  |

### 3.2 模块二：日志采集架构指导（6 个 API 端点）

**解决的问题**：企业日志分散孤岛、格式杂乱、不会部署集中采集、排查采集故障


| 功能         | 说明                                                      | 技术实现                                                       |
| ------------ | --------------------------------------------------------- | -------------------------------------------------------------- |
| 设备智能匹配 | 根据设备型号/类型自动匹配最优采集协议                     | 三级匹配：精确型号(95分)→类型匹配(80分)→模糊子串(50分)       |
| 采集方案生成 | 输出完整落地配置：设备端推送+服务端接收+格式解析+字段映射 | 4 种采集策略（Syslog/File/DB/Agent），工厂模式注册             |
| 故障智能诊断 | 针对日志丢失、格式错乱、传输中断等问题自动定位+修复方案   | 5 维度评分算法（关键词+Token重叠+协议+故障类型+设备），总分100 |
| 架构推荐     | 根据设备数量/日志量级/预算/团队能力推荐合适架构           | 3 种架构模板（轻量ELK/分布式ELK/企业SIEM）                     |

**4 种采集策略**：


| 策略                  | 支持设备                          | 协议               |
| --------------------- | --------------------------------- | ------------------ |
| SyslogCollectStrategy | 防火墙/WAF/IDS/IPS/路由器/交换机  | syslog UDP/TCP 514 |
| FileCollectStrategy   | 服务器/Web/Nginx/Apache           | file (Filebeat)    |
| DBSyncCollectStrategy | MySQL/PostgreSQL/SQLServer/Oracle | db_sync            |
| AgentCollectStrategy  | 堡垒机/HIDS/EDR/SIEM              | agent 转发         |

### 3.3 模块三：脚本生成与技术赋能（7 个 API 端点）

**解决的问题**：人工编写正则/检索语句/溯源脚本效率低、门槛高


| 功能         | 说明                                                  | 技术实现                                                     |
| ------------ | ----------------------------------------------------- | ------------------------------------------------------------ |
| 正则规则生成 | 根据攻防场景自动生成行业标准正则匹配规则              | 4 级级联：场景识别→模板匹配→RAG兜底→通用兜底              |
| ES 查询生成  | 生成完整的 Elasticsearch Query DSL，可直接用于 Kibana | 场景关键词评分→模板查找→时间过滤器构建→RAG兜底            |
| 平台选型推荐 | 根据设备数/日志量/预算/技能推荐最优平台               | 5 维评分：设备范围(25)+日志量(20)+预算(20)+技能(15)+需求(20) |
| 攻击链路溯源 | 从多条日志中提取攻击链，识别入口/扩散路径/最终目标    | 6 级攻击阶段 + 7 步溯源流水线 + 3 种搜索脚本自动生成         |
| 脚本优化纠错 | 对已有正则/ES查询进行纠错和优化                       | 正则：编译检查+通频符计数+捕获组；ES：JSON有效性+query字段   |

**攻击溯源 6 级阶段**：侦查探测 → 初始入侵 → 权限提升 → 横向移动 → 持久化驻留 → 数据窃取/破坏

### 3.4 模块四：合规审计基线（4 个 API 端点）

**解决的问题**：企业日志管理不合规、无审计基线、无自查标准、整改无方向


| 功能           | 说明                                            | 技术实现                                                                                        |
| -------------- | ----------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| 合规标准问答   | 精准回答等保2.0/网安法/数安法关于日志的硬性要求 | RAG 从合规审计库检索 + LLM 智能解读                                                             |
| 个性化基线生成 | 根据资产规模/业务类型自动生成专属监控基线       | 6 大监控场景 + 资产规模调整（<30台宽松/>500台严格）+ 行业适配                                   |
| 合规自查       | 对比现有运维现状与合规标准，自动筛查缺口        | 8 项硬编码检查 + 评分算法`score = max(0, 100 - (critical×30 + high×15 + medium×8 + low×3))` |
| 批量合规自查   | 支持多资产批量合规检查                          | 批量并行 + 汇总报告                                                                             |

**合规标准库覆盖**：

- 等保 2.0（8 项要求）
- 网络安全法（2 项要求）
- 数据安全法（2 项要求）

### 3.5 模块五：攻防实训考核（4 个 API 端点）

**解决的问题**：传统网安实训无反馈、不落地、无实战，学员不知道对错


| 功能           | 说明                                                | 技术实现                                                      |
| -------------- | --------------------------------------------------- | ------------------------------------------------------------- |
| 智能任务下发   | 自动加载场景资源，下发标准化实操任务                | 12 个实训场景（20+ 任务），含输入数据和预期输出               |
| 双维度答案校验 | 「规则精准匹配 + 语义相似度比对」双重校验           | SequenceMatcher 相似度 + LLM 灰色区间增强（[60,88) 区间触发） |
| 原理讲解与复盘 | 纠错后输出：错误原因+正确标准+底层攻防原理+实操要点 | 7 种攻击原理知识库（SSH爆破/SQL注入/横向移动/Webshell 等）    |
| 实训报告生成   | 全程记录操作轨迹，自动分析薄弱项+生成提升方案       | 6 类薄弱项检测 + 综合评级 A(≥90)/B(≥70)/C(<70)              |

**评分阈值体系**：


| 等级 | 分数  | 含义           |
| ---- | ----- | -------------- |
| A    | ≥90  | 直接通过       |
| B    | 70-89 | 提示优化       |
| C    | <70   | 判错并强制复盘 |

### 3.6 扩展模块：安全威胁狩猎（2 个 API 端点）

v3.0 新增，将日志联合审查升级为安全威胁狩猎：

- **关键词 + LLM 双引擎驱动**的多源日志关联分析
- 支持**多文件上传**，同时分析多个日志文件
- **22 种攻击链检测模式**，自动识别跨源攻击
- LLM + 关键词结果**智能合并**，避免重复告警
- **4 层容错解析机制**，确保 LLM 返回不完整 JSON 不会崩溃

---

## 四、RAG 知识库体系

### 4.1 五大知识库（259 条文档）


| 知识库     | 中文名     | 文档数 | 数据来源                                           |
| ---------- | ---------- | ------ | -------------------------------------------------- |
| log_basics | 日志基础库 | 8      | log_features, risk_rules                           |
| compliance | 合规审计库 | 10     | compliance_standards, baselines                    |
| collection | 采集架构库 | 5      | collect_templates, device_protocol, arch_templates |
| scripts    | 技术脚本库 | 59     | 9 个 script_gen_*.json                             |
| cases      | 实训案例库 | 177    | training_scenarios, fault_kb, correlation_patterns |

### 4.2 三级嵌入降级策略

```
Level 1: BGE 中文语义嵌入 (1024维)     — BAAI/bge-large-zh-v1.5
    ↓ 不可用时降级
Level 2: MiniLM 轻量英文 (384维)       — all-MiniLM-L6-v2
    ↓ 不可用时降级
Level 3: N-gram 纯 Python (128维)      — 字符 n-gram + MD5 哈希 + L2 归一化
```

### 4.3 知识库工程特性

- **自动导入**：首次启动自动将 21 个 JSON 文件导入 ChromaDB
- **增量更新**：已有数据自动跳过，`force=True` 可强制重建
- **线程安全**：单例 PersistentClient + SQLite 锁检测 + journal 文件清理
- **维度检测**：嵌入维度不兼容时自动重建集合

---

## 五、CLI 工具特色

CLI 工具是 Web 智能体的**完整功能镜像**，但采用同步实现，唯一运行时依赖为 `requests>=2.25`。

### 5.1 双模式交互


| 模式        | 说明                                                           |
| ----------- | -------------------------------------------------------------- |
| 菜单模式    | 8 个主菜单项，逐层引导操作                                     |
| AI 对话模式 | 自然语言输入，意图自动分类 → RAG 检索 → 模块调用 → 结果润色 |

### 5.2 命令行参数（16 个）

```bash
# 基础操作
log-guard --version                             # 查看版本号
log-guard --list-logs                           # 列出系统日志文件
log-guard -f /var/log/auth.log --parse          # 解析日志文件
log-guard -f auth.log -b --assess               # 批量解析 + 风险评估

# AI 问答
log-guard --ask "什么是SQL注入"                   # 非交互式 AI 问答
log-guard --ai                                   # 交互式 AI 对话模式

# 业务功能
log-guard --diagnose "SSH connection timeout"    # 故障诊断
log-guard --regex "detect SQL injection"         # 生成正则规则
log-guard --es-query "查找登录失败日志"            # 生成 ES 查询
log-guard --baseline 50                          # 合规基线生成
log-guard --qa "日志留存要求"                      # 合规问答
log-guard --train basic                          # 派发训练场景
```

### 5.3 AI 核心架构

```
用户输入 → 意图分类(7类) → RAG检索 → 模块调用 → 结果润色 → 返回
```

- **意图分类器**：LLM 分类，temperature=0.05，置信度阈值 0.40
- **RAG 引擎**：API 远程嵌入 + 本地 JSON 缓存 + SHA-256 哈希变更检测
- **结果润色器**：注入模块专用 Prompt + RAG context + 对话历史

---

## 六、前端设计亮点

### 6.1 技术选型

- Vue 3.5 + Element Plus 2.9 + TypeScript 5.7
- 无 vue-router：hash 路由 + `<component :is>` 手动切换（21 个路由）
- 无 Vuex/Pinia：组件级 `ref()` / `reactive()`，跨页用 sessionStorage/localStorage
- 暗色主题默认，6 个 CSS 文件分层管理

### 6.2 运维/实训双模式 UI

前端支持**运维模式（ops）**和**实训模式（training）**一键切换：


| 模式     | 可见模块                                            | 使用场景             |
| -------- | --------------------------------------------------- | -------------------- |
| 运维模式 | 日志解析/日志采集/脚本生成/合规审计/联合审查（5个） | 企业运维人员日常工作 |
| 实训模式 | 全部 6 个模块（额外显示攻防实训）                   | 学员培训考核         |

- 模式状态持久化到 `localStorage`，刷新后自动恢复
- 切换时自动处理页面跳转和引导教程

### 6.3 10 个可复用组件


| 组件                  | 功能                                           |
| --------------------- | ---------------------------------------------- |
| GlobalTour.vue        | 5 步新手引导教程                               |
| RiskBadge.vue         | P0-P3 风险等级标签（红/橙/黄/灰/绿）           |
| RiskCard.vue          | 风险详情卡片（含置信度、低置信警告、高危告警） |
| CodeBlock.vue         | 语法高亮代码块 + 一键复制                      |
| KnowledgePanel.vue    | 可折叠知识点面板                               |
| ConfirmBatch.vue      | 批量操作确认对话框                             |
| AlertGuide.vue        | 彩色提示条（info/warning/danger/success）      |
| EmptyGuide.vue        | 空状态占位                                     |
| ResultGuide.vue       | 可折叠结果解读                                 |
| CliDownloadBanner.vue | CLI 下载横幅                                   |

---

## 七、工程化设计模式

### 7.1 设计模式汇总


| 模式         | 实现位置                      | 价值                                               |
| ------------ | ----------------------------- | -------------------------------------------------- |
| **工厂模式** | 6 个工厂类                    | 新增场景仅需创建策略类 +`register()`，不改核心代码 |
| **策略模式** | 20+ 策略类                    | 每种设备/攻击/合规检查独立策略，互不干扰           |
| **单例模式** | 10+ 单例                      | 全局配置/日志/LLM/RAG 统一管理，避免资源浪费       |
| **模板方法** | BaseParser, BaseCheckStrategy | 父类定义流程骨架，子类实现具体步骤                 |
| **注册模式** | 所有工厂                      | 开闭原则：对扩展开放，对修改关闭                   |
| **外观模式** | 各 service.py                 | 封装内部复杂组件，暴露简洁 API                     |

### 7.2 模块零耦合约束

- 五大业务模块**禁止互相 import**
- 跨模块数据传递通过 `ContextManager`（请求级上下文隔离）
- 上游模块输出标准化 JSON Schema：`{ module_id, input, output, status, error_info }`
- 用户可从任一模块独立接入，上下文管理器自动补全依赖

### 7.3 配置管理


| 层级     | Agent 端                             | CLI 端                                              |
| -------- | ------------------------------------ | --------------------------------------------------- |
| 优先级   | 环境变量 > .env > settings.py 默认值 | 环境变量 > .env > ~/.log-guard/config.json > 默认值 |
| 配置文件 | pydantic-settings                    | 手写`.env` 解析器（无外部依赖）                     |
| 路径处理 | 基于`_PROJECT_ROOT` 计算绝对路径     | 多候选路径解析                                      |

---

## 八、数据资产

### 8.1 21 个 JSON 规则/知识文件


| 文件                             | 内容                                                      |
| -------------------------------- | --------------------------------------------------------- |
| `risk_rules.json`                | 7 条风险评估规则（P0-P3），含 condition/weight/confidence |
| `log_features.json`              | SSH/Web 关键词权重表（多特征加权识别）                    |
| `default_rules.txt`              | 10 条管道符分隔正则规则                                   |
| `correlation_patterns.json`      | 22 种攻击链检测模式（sequence/time_window）               |
| `fault_kb.json`                  | 9 种故障诊断知识（keywords/protocol_hints/fix_steps）     |
| `compliance_standards.json`      | 等保2.0(8项)+网安法(2项)+数安法(2项)                      |
| `compliance_baselines.json`      | 7 条合规基线模板（6大监控场景）                           |
| `arch_templates.json`            | 3 种架构模板（轻量ELK/分布式ELK/企业SIEM）                |
| `collect_templates.json`         | 4 种采集协议模板                                          |
| `device_protocol.json`           | 16 种设备协议映射                                         |
| `training_scenarios.json`        | 12 个实训场景（20+ 任务）                                 |
| `training_standard_answers.json` | 标准答案 + key_fields + 评分规则                          |
| `script_gen_*.json` (8个)        | 正则/ES查询/平台/溯源/评分等配置                          |

### 8.2 20 个 JSON 配置 + 1 个默认规则文件

全部外部化配置，零硬编码。新增场景仅需添加 JSON 文件 + 在工厂中注册，无需修改核心代码。

---

## 九、版本迭代详细对比

项目历经 4 个版本、10 天迭代，从零到一完成全栈交付。以下逐版本对比核心变化。

### 9.0 版本演进总览

```
v1.0.0 (7/18)  ──── 基础架构 + 5 大后端模块
  │                  46 文件变更, +4,126 行
  ▼
v2.0.0 (7/22)  ──── Vue3 前端 + RAG 知识库 + CLI 工具 + 联合审查
  │                  202 文件变更, +29,708 行 (最大增量版本)
  ▼
v2.1.0 (7/24)  ──── 交互优化 + 性能提升 + Bug 修复 + CLI 自动发布
  │                  65 文件变更, +8,421 行
  ▼
v3.0.0 (7/24)  ──── 安全威胁狩猎 + 多文件关联 + LLM 容错
                     14 文件变更, +1,262 行
```

---

### 9.1 v1.0.0 — 基础架构奠基（2026-07-18）

> **定位**：从零搭建项目骨架，完成 5 大核心业务模块的后端实现

**代码规模**：46 个文件变更，+4,126 行代码

**核心交付**：


| 维度             | v1.0 交付内容                                          |
| ---------------- | ------------------------------------------------------ |
| 模块一：日志解析 | 5 种解析器（SSH/Web/WAF/Firewall/DB）+ 风险基线评估    |
| 模块二：日志采集 | 设备匹配 + 采集方案 + 故障诊断 + 架构推荐              |
| 模块三：脚本生成 | 正则生成 + ES 查询生成 + 平台选型 + 攻击溯源           |
| 模块四：合规审计 | 基线生成 + 合规自查（基础版）                          |
| 模块五：攻防实训 | 任务下发 + 答案校验 + 报告生成                         |
| 工程规范         | 工厂注册模式 + Pydantic 校验 + 外部化配置 + 模块零耦合 |
| API              | 25 个 REST 端点                                        |
| 测试             | Agent 133 个 + CLI 236 个 = 369 个单元测试             |

**架构特点**：

- 三层通用架构（规则→RAG→LLM）已设计完成，但 RAG 知识库尚未导入
- 后端纯 API 服务，无前端界面
- CLI 工具功能完整，但交互为纯菜单模式

**关键提交**：

- `dc40103` Phase 1a+1b — 项目底座 + 日志解析模块（5种解析器）
- `2261876` 模块二 — 日志采集架构指导模块
- `96157c8` Phase 2 — 模块四脚本生成模块

---

### 9.2 v2.0.0 — 全栈能力跃升（2026-07-22）

> **定位**：从"后端 API 服务"升级为"全栈智能体平台"，补齐前端、RAG、CLI 三大短板

**代码规模**：202 个文件变更，+29,708 行（**最大增量版本，占总代码量 78%**）

**与 v1.0 对比的核心变化**：


| 维度           | v1.0             | v2.0                                             | 变化                     |
| -------------- | ---------------- | ------------------------------------------------ | ------------------------ |
| **前端**       | 无               | Vue3 + Vite + Element Plus（21个页面、10个组件） | 新增 5,000+ 行前端代码   |
| **RAG 知识库** | 设计完成，未导入 | ChromaDB 5 库 259 条文档 + 三级嵌入降级          | 新增 21 个 JSON 配置文件 |
| **CLI**        | 基础菜单模式     | 双模式（菜单+AI对话）+ 意图分类 + 结果润色       | AI 核心架构重写          |
| **联合审查**   | 无               | 新增攻击链检测模块（22种模式）                   | 新增模块                 |
| **合规审计**   | 基础自查         | RAG 知识库检索 + LLM 智能解读                    | 三层架构完整落地         |
| **故障诊断**   | 简单关键词匹配   | 5维度评分算法 + LLM 降级                         | 混合架构升级             |
| **API**        | 25 个            | 29 个（+联合审查 2 个 + 实训流式分析 1 个）      | +4 个端点                |
| **CI/CD**      | 无               | GitHub Actions 自动测试 + CLI 自动打包发布       | 新增                     |
| **测试**       | 369 个           | 369 个（Agent 测试路径修复，ChromaDB 超时解决）  | 稳定性修复               |

**前端从零到一**：

- 21 个页面组件覆盖全部 5 大模块
- 10 个可复用组件（风险标签、代码块、引导教程等）
- 暗色主题 + 响应式设计
- 运维/实训双模式一键切换
- 统一 API 层（18 个方法对应 29 个端点）

**RAG 知识库从设计到落地**：

- 新增 `kb_ingest.py` 知识库导入引擎（193行），自动将 JSON 展平为向量
- `vector_store.py` 从 51 行扩展到 385 行，实现三级嵌入降级
- 首次启动自动导入 21 个文件到 5 个 ChromaDB 集合
- 线程安全单例 + SQLite 锁检测 + 维度不兼容自动重建

**CLI 从菜单到 AI 对话**：

- 新增 `ai_core/` 目录（7 个文件），实现完整 AI 调度链
- 意图分类器（7类意图，LLM 分类）
- RAG 引擎（API 远程嵌入 + 本地 JSON 缓存 + SHA-256 变更检测）
- 结果润色器（模块专用 Prompt + 对话历史注入）
- 总调度器（用户输入→意图分类→RAG→模块→润色→返回）

**关键提交**：

- `991828b` 新增 Vue3 前端项目框架
- `6c3fede` 实现 RAG 知识库数据导入
- `da2bb8c` 向量库嵌入能力增强，三级降级策略
- `150ad8e` CLI v2.1 全面优化（自然语言输出、格式化器重写）
- `c96a42d` 新增 CLI 联合日志审查模块
- `4d3fd00` compliance 模块接入 RAG + LLM 智能解读

---

### 9.3 v2.1.0 — 交互打磨与性能优化（2026-07-24）

> **定位**：用户体验优化 + 关键性能提升 + Bug 修复，让产品从"能用"到"好用"

**代码规模**：65 个文件变更，+8,421 行

**与 v2.0 对比的核心变化**：


| 维度         | v2.0            | v2.1                                              | 变化         |
| ------------ | --------------- | ------------------------------------------------- | ------------ |
| **实训性能** | 提交后分析 ~30s | timeout 30s→15s，A级跳过LLM                      | 速度提升 50% |
| **日志识别** | 单行输入        | 多行输入，逐条分别识别                            | 交互增强     |
| **批量解析** | 基础结果        | 增加风险说明、攻击类型、处置建议                  | 信息丰富度   |
| **实训报告** | 仅显示分数      | 低分任务可展开查看标准答案+解析+提示              | 学习体验     |
| **提交校验** | 无必填校验      | 三题全填才能提交                                  | 防误操作     |
| **页面联动** | 独立页面        | 识别→解析→研判三页面打通分析链路                | 流程贯通     |
| **CLI**      | 基础功能        | 格式化器重写 + 通用日志解析器 + 合规匹配改进      | 输出质量     |
| **发布**     | 无              | GitHub Actions 自动打包 + CLI 下载横幅            | 分发能力     |
| **Bug 修复** | —              | 修复 3 个崩溃 bug + 8 个页面缺失字段 + LLM 不工作 | 稳定性       |

**性能优化细节**：

- 实训模块 A 级任务（≥90分）直接出报告，跳过 LLM 调用
- Prompt 精简，减少 token 消耗
- 合规基线生成超时从 30s 降至 15s

**交互改进细节**：

- 日志识别页面支持多行粘贴，逐条自动识别
- Assess/Parse 页面支持 `hashchange` 重新激活时自动填充
- 8 个前端页面补充之前缺失的字段展示
- 实训报告增加"查看标准答案"展开区域

**关键提交**：

- `fcf1ac1` 提交后分析速度优化 — timeout 30s→15s
- `b154b2a` 重构日志识别-解析-研判三个页面，打通分析链路
- `2a18bcc` CLI 下载横幅 + GitHub Actions 自动打包发布
- `4309b8a` 实训报告添加参考答案和解析

---

### 9.4 v3.0.0 — 安全威胁狩猎升级（2026-07-24）

> **定位**：联合审查模块从"日志关联"升级为"安全威胁狩猎"，引入 LLM 双引擎驱动

**代码规模**：14 个文件变更，+1,262 行

**与 v2.1 对比的核心变化**：


| 维度           | v2.1             | v3.0                                         | 变化         |
| -------------- | ---------------- | -------------------------------------------- | ------------ |
| **模块定位**   | 日志联合审查     | 安全威胁狩猎                                 | 能力升级     |
| **分析引擎**   | 关键词匹配单引擎 | 关键词 + LLM 双引擎                          | 智能性提升   |
| **文件上传** | 单文件           | 多文件同时上传关联分析                       | 场景扩展     |
| **前端联动**   | 静态展示         | 实时联动展示                                 | 交互升级     |
| **攻击链规则** | 基础规则         | 重写 correlation_patterns.json，覆盖更多场景 | 规则增强     |
| **容错机制**   | 无               | 4 层 JSON 截断恢复 + LLM+关键词智能合并      | 鲁棒性       |
| **service.py** | 594 行           | 1,028 行（+434 行，重构）                    | 核心逻辑重写 |

**4 层容错解析机制**（v3.0 新增）：

1. 完整 JSON 解析
2. 截断修复（补全未闭合的括号/引号）
3. 正则提取 JSON 片段
4. 兜底返回关键词分析结果

**LLM + 关键词智能合并**：

- 两路结果按实体键（src_ip）去重合并
- LLM 结果优先级高于关键词结果
- 避免重复告警，提升告警质量

**关键提交**：

- `bb9b126` 安全威胁狩猎改造 — 关键词+LLM双引擎 + 前端联动 + 文件上传
- `97446ef` LLM+关键词合并模式、多文件上传、LLM prompt优化
- `911dc3d` LLM JSON截断恢复 + 4层容错解析

---

### 9.5 各版本数据对比


| 指标          | v1.0  | v2.0         | v2.1  | v3.0  | 累计   |
| ------------- | ----- | ------------ | ----- | ----- | ------ |
| 发布日期      | 7/18  | 7/22         | 7/24  | 7/24  | —     |
| 文件变更数    | 46    | 202          | 65    | 14    | 236    |
| 代码新增行    | 4,126 | 29,708       | 8,421 | 1,262 | 38,058 |
| 后端模块数    | 5     | 6(+联合审查) | 6     | 6     | 6      |
| API 端点数    | 25    | 29           | 29    | 29    | 29     |
| 前端页面数    | 0     | 21           | 21    | 21    | 21     |
| JSON 配置文件 | 8     | 21           | 21    | 21    | 21     |
| 单元测试数    | 369   | 369          | 369   | 369   | 369    |
| RAG 文档数    | 0     | 259          | 259   | 259   | 259    |
| 攻击链模式    | 0     | 22           | 22    | 22    | 22     |

### 9.6 每版本解决的核心问题


| 版本 | 核心问题                      | 解决方案                                          |
| ---- | ----------------------------- | ------------------------------------------------- |
| v1.0 | 如何快速搭建 5 大安全模块？   | 工厂+策略模式，模块零耦合，外部化配置             |
| v2.0 | 如何让 API 服务变成完整产品？ | Vue3 前端 + RAG 知识库杜绝幻觉 + CLI 工具 + CI/CD |
| v2.1 | 用户用起来哪里不顺？          | 实训提速 50% + 页面链路打通 + Bug 修复 + 交互打磨 |
| v3.0 | 关联分析不够智能？            | LLM 双引擎 + 多文件 + 4 层容错 + 智能合并         |

---

## 十、测试体系

### 10.1 测试规模

```
Agent 单元测试: 133/133 全部通过 (耗时 10.41s)
CLI 单元测试:   236/236 全部通过 (耗时 0.78s)
总计:           369/369 全部通过
```

### 10.2 测试覆盖矩阵


| 测试文件                     | 测试数 | 覆盖内容                                          |
| ---------------------------- | ------ | ------------------------------------------------- |
| test_api.py                  | 11     | 全部 API 端点集成测试                             |
| test_script_gen.py           | 48     | 5 种脚本策略 + 配置加载                           |
| test_log_collect.py          | 31     | 采集策略工厂 + 设备匹配 + 故障诊断                |
| test_log_parse.py (CLI)      | 38     | 6 种解析器 + 工厂 + 服务                          |
| test_all_features_capture.py | ~60    | 全功能集成演示（6种解析器+14项功能）              |
| 其他测试文件                 | ~180   | 工具函数、配置、LLM客户端、意图分类、上下文管理等 |

### 10.3 CI/CD


| 工作流                              | 触发条件                | 功能                                          |
| ----------------------------------- | ----------------------- | --------------------------------------------- |
| `.github/workflows/ci.yml`          | push/PR to main         | CLI + Agent 并行测试，Python 3.11             |
| `.github/workflows/release-cli.yml` | push to main（CLI变更） | 自动 zip 打包 → 日期版本号 → GitHub Release |

---

## 十一、项目数据总览


| 指标          | 数值                                      |
| ------------- | ----------------------------------------- |
| 开发周期      | 10 天（2026-07-14 ~ 2026-07-24）          |
| Git 提交      | 78 次                                     |
| 代码变更      | 236 个文件，38,058 行新增                 |
| Python 文件   | 571 个                                    |
| 前端源文件    | 47 个（Vue/TS/JS）                        |
| 业务模块      | 5 个核心 + 1 个扩展（安全威胁狩猎）       |
| API 端点      | 29 个                                     |
| 前端页面      | 21 个路由                                 |
| 可复用组件    | 10 个                                     |
| JSON 配置文件 | 21 个                                     |
| RAG 知识库    | 5 库 259 条文档                           |
| 单元测试      | 369 个（全部通过）                        |
| 设计模式      | 6 种（工厂/策略/单例/模板方法/注册/外观） |
| 日志解析器    | 5 种（SSH/Web/WAF/Firewall/DB）           |
| 采集策略      | 4 种（Syslog/File/DB/Agent）              |
| 攻击链模式    | 22 种                                     |
| 实训场景      | 12 个（20+ 任务）                         |
| 风险规则      | 7 条（P0-P3）                             |
| 合规标准      | 等保2.0 + 网安法 + 数安法（12 项要求）    |

---

## 十二、项目特色亮点总结

### 亮点一：三层通用架构，兼顾准确性与智能性

规则引擎保证基础功能 100% 准确，RAG 知识库精准召回安全领域知识杜绝幻觉，LLM 仅在必要时触发做深度解读。三层逐级降级，系统始终可用。

### 亮点二：模块零耦合，工厂+策略模式极致扩展

五大业务模块禁止互相 import，通过 ContextManager 传递数据。新增日志类型/攻击场景/合规规则，仅需创建策略类 + 在工厂注册，零修改核心代码。

### 亮点三：三级嵌入降级，环境无关部署

BGE → MiniLM → N-gram 三级降级，从需要 GPU 的语义嵌入到纯 Python 字符级向量，确保在任何环境下 RAG 知识库都能工作。

### 亮点四：实训双维度校验，真正的"AI教练"

规则精准匹配 + LLM 语义评分双重校验，灰色区间（60-88分）触发 LLM 深度分析，7 种攻击原理知识库自动讲解，实现"纠错+原理+复盘"闭环。

### 亮点五：运维/实训双模式一键切换

同一平台服务企业运维人员和学员培训，通过前端模式切换自动过滤模块可见性，状态持久化，切换无感。

### 亮点六：CLI 工具零依赖发布

CLI 唯一运行时依赖 `requests>=2.25`，GitHub Actions 自动打包发布，`pip install -e .` 即可使用，降低分发门槛。

### 亮点七：10 天完成全栈交付

从零到一，10 天完成 38,000+ 行代码、571 个 Python 文件、47 个前端文件、369 个测试、29 个 API、5 大业务模块的全栈交付，展现了高效的 AI 辅助开发能力。
